# Hatiwal — Mobile → Web Feature Migration Catalog

> **What this is:** a complete, line-by-line catalog of **every mobile feature** and its status on the
> **web** client (`hatiwal-web/`, Next.js). It is the source of truth for "what still needs porting
> from the app to the web," and the **work queue the software house pulls `web` tasks from**.
>
> **Read alongside:** [`../../docs/MOBILE_WEB_PARITY.md`](../../docs/MOBILE_WEB_PARITY.md) (page-by-page
> parity map) · [`prompts/web.prompt.md`](prompts/web.prompt.md) (how to build web features the house way)
> · `hatiwal-mobile/docs/BACKLOG.md` (mobile feature detail).

## The honest headline

**Web is NOT starting from zero.** As of the 2026‑06‑21 parity audit the web client already reaches
near-full parity with mobile (browse, auth, seller dashboard, maps, real-time chat, offers, reports,
saved searches, account delete/restore, buyer/seller mode). So this is **not "port 100 net-new
features."** It is a **~110-feature catalog** where most core features are ✅ done, ~10 are 📱 mobile-only,
and the **real migration work is ~30 gaps** — mostly features mobile shipped *after* that audit
(delete-message, nearest sort, saved-by-N, message search, price-drop / response-rate badges, archive /
mark-unread, away mode, counter-offer, etc.).

> ⚠️ **Verify-before-build:** the ✅/⬜ status below is derived from the 12-day-old parity audit + the
> mobile inventory, not a fresh line-by-line read of every web route. A `web` task's **first step** is to
> confirm the gap still exists in `hatiwal-web/` before building (some may have landed since the audit).

### Legend

| Mark | Meaning |
|---|---|
| ✅ | Already built on web — parity confirmed by the audit |
| ⬜ | **GAP — needs porting to web** (the migration work; becomes a `web` board task) |
| 📱 | Mobile-only by nature — do **not** port 1:1 (rebuild with a web equivalent only if it matters) |
| ❓ | Uncertain — web dev must verify against current web code first, then build if missing |
| 🌐 | Web already has this natively / for free (e.g. real URLs, SSR) |

---

## 0 — Onboarding & Bootstrap

| ID | Feature | Web | Migration note |
|---|---|---|---|
| A3 | App bootstrap / splash redirect | ✅ | Web hydrates auth on load; native splash is 📱 N/A |
| A4 | Guest browsing + auth gate (`returnTo`) | ✅ | `GUEST_BROWSE_AND_AUTH_REDIRECT.md` |
| W924 | First-run onboarding welcome carousel | ⬜ | Optional web first-visit welcome (dialog/hero), gated by a cookie/localStorage flag. Low priority |

## A — Authentication

| ID | Feature | Web | Migration note |
|---|---|---|---|
| A1 | Login | ✅ | `/login` |
| A2 | Register | ✅ | `/signup` |
| A-FP | Forgot / reset password | ✅ | `/forgot-password`, `/reset-password` |
| A-SO | Sign out | ✅ | profile + header dropdown |
| A-LANG | Language switcher (en/ps/fa) | ✅ | header locale switcher |

## B — Buyer: Browse & Discover

| ID | Feature | Web | Migration note |
|---|---|---|---|
| B1 | Browse (Bazaar) feed + infinite scroll | ✅ | `/bazaar` |
| B3 | Keyword search | ✅ | |
| B3-HISTORY | Recent-search memory (last 10 terms) | ✅ | One client-only store `src/lib/use-search-history.ts` (localStorage `hatiwal.searchHistory`; trim · min 2 chars · case-insensitive dedupe · newest-first · cap 10 — mobile's `searchHistory.store.ts` rules) rendered by the shared `components/shared/search-history-panel.tsx` from BOTH fields through `components/shared/search-box.tsx`. ONE rule, ONE presentation: each field floats the chips as a dropdown under itself while THAT field is focused and empty — so with the header field and the Bazaar field both on screen at `lg`+ the same history can never show twice, the block never pushes the Bazaar filters down (nor shifts the column when the client-only history hydrates) and Escape/blur always dismisses it. Floating also means the panel owes nothing to its host field's width, and it is deliberately allowed to be **wider** (`w-[max(100%,min(24rem,calc(100vw-2rem)))]`, anchored `start-0` so it mirrors in RTL): matching the Bazaar's 250px column stacked all ten terms one per row with most of them behind a scroll, where 24rem fits two chips to a row and puts the whole capped history on screen. The Bazaar's own field is `lg`+ only; below that the header's field is the one on screen and it seeds + re-syncs from `?q=` (`popstate` included), so the visible box always carries the active query at every width. At `lg`+ BOTH fields are on screen, and web is the only client with that problem (mobile's Browse has exactly one field) — so whichever field commits a query publishes it through the tiny module store `src/lib/use-browse-query.ts` (`useSyncExternalStore`, `getServerSnapshot` = nothing published yet, versioned snapshot so re-searching the same term is still news) and every other field adopts it. The URL is still the source of truth but cannot be the channel: the Bazaar commits with `router.replace()` on the SAME pathname, which fires no `popstate` and only reaches `window.location` once the RSC payload lands, so before this the app's most prominent search box could sit there advertising a term the feed was NOT filtered by (refine or clear in the sidebar → the header kept the old query, X and all). The Bazaar publishes from its one filters→URL effect, so every path — settled debounce, Enter, a recent-search chip, "Reset filters", an applied saved search — is covered by construction. Keyboard: ArrowDown enters the chips, arrows rove (RTL-aware), Escape returns the caret; the input points `aria-controls`/`aria-describedby` at the panel and an `sr-only` hint while it is open (deliberately not a combobox — each chip owns a Remove button). Chip re-runs the search through the existing `?q=` path. No API, no auth, nothing new in the URL — works logged out. 20 Playwright specs in `e2e/search-history.spec.ts` — including both directions of the two-field sync at 1280px (refine + clear in the Bazaar field, and a search made in the header) and the 250px panel's readability geometry |
| B3b | Category filter chips + picker | ✅ | |
| B7 | Item-condition filter | ✅ | |
| B-PRICE | Price-range filter | ✅ | |
| B5 | Map location & distance filter | ✅ | Leaflet; ⚠️ GPS uses browser Geolocation, not `expo-location` |
| B-SORT | Sort: newest/oldest/price↑↓ | ✅ | |
| B931 | Sort: **Most viewed** | ⬜ | Add `mostViewed` sort option to `/bazaar` sort control + wire `sort` param |
| D583 | Sort: **Nearest first** | ✅ | Added `nearest` sort option on `/bazaar`; acquires a browser-Geolocation fix (denial/failure → friendly toast + clean fallback to default sort) and sends lat/long with `sort=nearest`. Reuses the existing `GET /listings` (Rails `sort=nearest`). Coords kept ephemeral/client-only (never in the URL) so reload falls back cleanly. 3 locales · RTL · dark |
| B617 | "Active sellers" filter (signed in ≤7d) | ⬜ | Add toggle chip to `/bazaar` filters; param already served |
| C481 | "Filters active (N) · Clear all" pill | ❓ | Verify the redesigned filter sidebar already shows an active-count + clear-all; add if missing |
| B4 | Saved searches / filter history | ✅ | |
| N612 | Saved-search "new matches" badge | ✅ | Badge + mark_seen on the browse saved-searches sidebar |
| B6 | "Seen / already viewed" indicator | ⬜ | Dim card + "Seen" badge from per-user `ListingView`; endpoint exists |
| B-VIEW | Grid / list view-mode toggle | ⬜ | Persisted grid↔list toggle on `/bazaar` |
| B-STATES | Feed states (skeleton/empty/error) | ✅ | |

## Categories

| ID | Feature | Web | Migration note |
|---|---|---|---|
| C736 | Categories hub (grid + live counts) | ✅ | `/categories` (also SEO landing pages) — one `categories?with_counts=true` call feeds the counts + children; a parent's count **rolls its subcategories up** (matches what `?category_id=` returns); empty categories are de-emphasised and sorted last; skeleton/empty/error states |
| S417 | Subcategory drill-down | ✅ | `CategoryBadge` chips (44px, own count) on each hub card → `/categories/<child-slug>`, overflowing to "+N more"; Bazaar sidebar nests children under the selected parent (`?category=<child-slug>`) |

## B2 — Listing Detail (buyer view)

| ID | Feature | Web | Migration note |
|---|---|---|---|
| B2 | Listing detail (increments views) | ✅ | `/listings/[id]` |
| B2-GAL | Image gallery carousel | ✅ | |
| B2-SELLER | Seller card → public profile | ✅ | |
| B2-PHONE | Seller phone reveal | ✅ | ⚠️ web = reveal + `tel:`/copy link, no native dialer |
| B2-MSG | "Message seller" CTA → first message | ✅ | |
| B2-OFFER | Make an offer (quick-amount chips) | ✅ | |
| B2-SAVE | Save-heart (optimistic) | ✅ | |
| B2BAR | **Sticky mobile action bar** (price + Message seller + Save) | ✅ | `listing-action-bar.tsx` — web port of mobile's sticky bottom CTA. Client island, `lg:hidden` **and** gated on `matchMedia("(max-width: 63.999rem)")` — the rem complement of Tailwind's `lg`, so the JS gate and the class can't disagree — so it is never in the desktop DOM (verified: present at 844/1000/1023px, absent at 1024). One `IntersectionObserver` watches two targets and pins the bar only while NEITHER is on screen: `#listing-actions` (don't duplicate a CTA the buyer is already looking at) and `[data-site-footer]` (a fixed bar would cover privacy / delete-account with nothing left to scroll). Carries exactly price + Message + Save: reuses `StartConversationButton` (`layout="bar"` → compact row, **no** offer button — a second 40px control left 98px of box for a 125px label on a 360px phone) + `SaveButton` (`variant="bar"`) verbatim, so conversation/save state is shared with the inline block, never duplicated. Returns null for the seller's own listing (`useIsOwner` — no clash with `OwnerListingBar`) and for non-active listings (the `UnavailableActions` recovery CTAs own that case), and it renders its own height spacer so those three no-bar cases get no dead space above the footer. Slides via `bottom`, and the frosted background is a separate layer — `transform`/`backdrop-filter` would make the fixed bar the containing block for the message dialog. `inert` while hidden. One new key `listing.detail.actionBarLabel` in en/ps/fa; RTL mirrors (price on the start side) + dark; 10 Playwright specs (`e2e/listing-action-bar.spec.ts`).<br>**Independent review (cycle 5):** all 11 specs re-run green (serially — 8 parallel workers against a dev server flake on page loads, not on the bar), `tsc`/eslint clean. One real defect found and fixed: the shared `Dialog` is **not portalled**, so the message/offer dialog opened from the bar is a *descendant* of the bar — anything that unpinned the bar mid-compose (rotating the phone, an iOS scroll-behind that `body{overflow:hidden}` doesn't hold, any reflow that brings `#listing-actions` into view) faded the open dialog to `opacity-0`, marked it `inert` and slid the half-typed message off screen. `StartConversationButton` now reports its dialog state through an optional `onDialogOpenChange`, and the bar holds still (`shown = pinned \|\| dialogOpen`) while it hosts an open dialog, handing control back to the observer on close. Regression spec: "keeps its dialog alive when the bar would otherwise slide away"<br>**Independent review (cycle 6) — 4 defects fixed:** **(1) The bar's heart and the inline heart diverged.** Both `SaveButton`s live for the whole page (the bar is CSS-hidden, never unmounted) and each held its own `override`, so unsaving in one place left the other filled *permanently* and its next tap fired DELETE at an already-unsaved listing. The optimistic flip now lives in ONE cache entry — `['saved-flips', userId]`, written by a `useMutation` `onMutate` with per-listing rollback, `['saved-listings']` invalidated on success and the flip dropped once the refetched list confirms it — so every heart for a listing reads one source (viewer-scoped because logout does not clear the query cache). A write into `['saved-listings']` itself can only express *unsave*: adding needs a whole `Listing`, which the id-only call sites do not have, and a stub would render as a broken card on /saved. Also fixed the pre-existing cause of wrong hearts: `getSavedListings()` fetched page 1 only, so a buyer with >20 saves saw an OUTLINE heart on a listing they had saved — it now pages to the end like `getMyListings()`. **(2) Crossing `lg` mid-compose destroyed the half-typed message** — `if (!compact) return null` unmounted the bar's whole subtree (rotating a tablet, dragging a desktop window across 1024px), and the inline block's button is a different instance with empty state. The shared `<Dialog>` is now **portalled to `<body>`**, so no dialog is ever a descendant of an animated, hidden, conditionally-rendered or `z-40`-capped host; the bar additionally stays mounted while it hosts an open dialog. That retires the "never put `transform`/`backdrop-filter` on the bar" constraint, so the separate frosted layer moved back onto the bar itself. **(3) The dialog-open report could latch true** and pin the bar over the footer forever: `StartConversationButton` reported `open || offerOpen` even after its `isOwner` / `status !== "authed"` early returns had stopped rendering the dialogs (logout mid-compose), so the report is now gated on `dialogsMounted` and cleaned up on unmount. **(4) a11y:** closing the bar's dialog restored focus to a button that was about to be `inert`, dropping the caret at `<body>` so the next Tab restarted at the top of the document — the bar now hands focus to the inline CTA it defers to. 14 specs (3 new: both hearts are one state, survives crossing `lg` mid-compose, focus handoff on close); the 10 dialog-heavy suites re-run for the portal change<br>**Reviews (cycles 7–9) — the bar's three controls stopped guessing, and the price stopped appearing twice.** **(1) Indeterminate ≠ "not saved".** On a cold load the heart's truth is two SEQUENTIAL round-trips away (`/api/auth/session`, then `/api/me/my/saved_listings`), and `initialSaved` cannot bridge it — this page's payload is fetched anonymously, so `isSaved` is `false` even for a listing saved months ago (only `true` is trustworthy there). The pinned heart therefore used to paint the OUTLINE heart labelled "Save listing" for ~1s+ and fire `POST save` on an already-saved listing, so a buyer who wanted to unsave had to tap twice; a failed list fetch claimed "not saved" forever with no toast and no retry. `SaveButton` now models a third state: while `status === "loading"`, the shared list is `isPending`, or it `isError`, the heart is `unknown` — muted fill, `aria-busy`, **no** `aria-pressed` — and a tap there is *queued* and replayed against the resolved value (a failed list is refetched on that tap, and the toast + release only come if the retry fails too). **(2) The CTA did the same thing, louder.** While auth was still resolving `StartConversationButton` rendered its GUEST branch — an `<a href="/login">` labelled "Message Seller", pinned from the first paint — so a signed-in buyer who tapped it was navigated off the listing to a login page they don't need. It now renders a real button in the same unsettled chrome (dimmed, `aria-busy`, `aria-disabled` once a tap is held) and replays the tap into the message dialog when the identity lands. Both controls share one contract: never render byte-identical to the ready state while a tap cannot be honoured, and never assume an answer. **(3) No double price.** The bar used to carry the price from the first paint while the hero price was still on screen — measured at 390x760, "AFN 30,000" twice, 120px apart. A separate `priceAnchorId` (`#listing-price`) now governs the bar's price *only*: it is added to the same observer's targets and the price renders only while the hero one is off screen. Deliberately not part of the hide set — the hero price ends at y=651 and `#listing-actions` starts at y=1201, a 550px gap inside a 760px viewport, so gating the whole bar on the price would leave no scroll position where it ever appears, deleting the feature (mobile makes the same trade: its sticky bar carries no price at all). **(4) Tablet slab.** `flex-1` alone stretched the primary to a 566x40 banner between 640px and 1023px; capped with `sm:max-w-xs md:max-w-sm` + logical `sm:ms-auto` so it groups against the heart as a toolbar. **(5) Spacer drift.** The hand-written `calc(4rem + safe-area)` was 1px short of the real 65px bar and would desync silently the next time the row grew — the bar now publishes its measured height via `ResizeObserver` and the spacer consumes it (the class is only the first-paint fallback). Not done, and deliberately: mobile keeps its bar on reserved/sold with a muted notice, but this card's acceptance requires the bar to be ABSENT there, and the inline `UnavailableActions` + hero `StatusBadge` already carry the status — a parity idea for a follow-up card, not a defect. **19 specs** (`e2e/listing-action-bar.spec.ts`), 5 new: the two-prices invariant at every scroll depth, the tablet cap, the queued unsave, the queued tap during auth bootstrap, and the CTA that must not bounce a signed-in buyer to /login (all mutation-checked against the pre-fix code). The two-prices spec was also de-flaked: an IntersectionObserver gate cannot be frame-exact, so the price/hero biconditional is asserted once settled instead of on the raw first sample, which failed 2 runs in 3 |
| G274 | Listing map snippet | ✅ | Leaflet |
| B173 | Similar-listings rail | ✅ | Bottom of `/listings/[id]` (no separate route) — `getSimilarListings(id)` → the dedicated `GET /listings/:id/similar` endpoint mobile uses (`Listing.similar_to`: browsable only, same category **and its children**, source excluded, newest first, max 8), rendered through the shared `listing-rail.tsx`; heading `listing.detail.similarListings`, "view all" → `/categories/[slug]` (see M547) |
| M547 | **"More from this Seller" rail** | ✅ | Second cross-sell rail on `/listings/[id]`, above Similar listings: `getListings({ userId: seller.id, status: 'active' })` (one extra request, skipped when there is no seller), current listing filtered out, capped at 4, trailing "view all" → `/sellers/[id]`. **Buyer-only** — wrapped in the new `components/auth/owner-gate.tsx` (`HideForOwner` + `useIsOwner`), mirroring mobile's `!isOwnListing` gate (`ListingDetail.tsx`): a seller must not be cross-sold their own stock under buyer copy with a "view all" to their own profile. The gate is the only client code; the rail stays a Server Component. `owner-gate.tsx` is now THE single owner test, reused by `SaveButton`, `HideListingButton`, `SafetyTips` and `OwnerListingBar` (the same 3-clause check was written out in each). On a sold/reserved listing the rail drops its own "view all": `UnavailableActions` already owns a prominent, name-carrying CTA to that profile (one destination, one CTA). Every titled listing section on the site now renders through shared `listing-rail.tsx` — both detail rails **and the home page's "Recent listings"** (new `size="lg"` typography variant + `priorityCount` + `empty` slot for its `EmptyState`); it renders nothing when empty and has no `empty`, so a rail can be dropped in unconditionally. This also fixed the similar rail's mislabeled heading (`home.recent` → `listing.detail.similarListings`). Both rails cap at 4 cards and `listing-rail.tsx` renders the `sm` size on the new `rail` column tracks (`grid-cols-2 md:grid-cols-4`) — 4 divides evenly into 2 and 4, so a rail never wraps a lone orphan card or leaves a hole in its last row (the page tracks' `sm:grid-cols-3`/`xl:grid-cols-5` did both); the rail's "view all" is a default-size (40px) Button, not `sm` (36px). Key `listing.detail.moreFromSeller` mirrors mobile; 3 locales; RTL (`rtl:-scale-x-100` arrow, logical spacing) + dark; 14 Playwright specs in `e2e/listing-detail.spec.ts` (rail contents · current listing excluded · ordering · dedupe · divider · a11y names · locale prefix in ps · owner-hidden + guest/other-seller controls · single seller CTA on sold/reserved) + a home-page assertion locking the shared rail in.<br>**Cycle 4:** both rails moved into one async Server Component, `components/listing/cross-sell-rails.tsx`, wrapped by the page in `<Suspense>` — the photo/price/seller column no longer waits on two cross-sell requests, and `CrossSellRailsSkeleton` (heading bar + 4 card skeletons on the same `rail` tracks) reserves the space while they stream. Similarity now comes from the dedicated endpoint (B173) instead of a client-side `categoryId` approximation, so web and mobile answer "what is similar to this?" with the same SQL; the seller fetch dropped from `pageSize: 12` to `RAIL_SIZE + 1` (the only reason to fetch past the cap is that this listing may come back in the page). The two rails are deduplicated through a `Set` of already-shown ids — the seller rail runs first and wins, so a seller's other listing in this same category is never offered twice under two headings (and if it was the similar rail's only candidate, that rail correctly doesn't render). A `Separator` marks the boundary between the two grids, inside the owner gate so it can't dangle. The similar rail got the symmetric "view all" → `/categories/[slug]` (active listings only — on sold/reserved `UnavailableActions` owns both CTAs). A11y: each rail is a named `region` (`aria-label={title}`) and "View all" carries the section title `sr-only` **after** the visible words (WCAG 2.5.3), so two adjacent rails aren't two identical links. `ListingCard` gained `tracks` (`CARD_IMAGE_SIZES`, typed by the same key as the grid's `GRID_COLUMNS`) so a rail card's `sizes` describes the rail's 2→4 layout, not the feed's 2→5. Backend (both clients benefit): `Listing.similar_to` now expands through `by_category` (self_and_children) so an item filed on a parent category still gets a rail, and the `similar` action applies `not_hidden_for(current_user)` so the rail honours "Not interested" — model + request specs added. **Known gap:** on web the rails are fetched anonymously (public payloads never carry the devise token — see `lib/api/client.ts`), so a signed-in buyer's hidden listings can still appear there; the browse feed and category hubs share that gap, and closing it belongs in the fetch layer once for all of them |
| B7b | Condition badge | ✅ | |
| N804 | **Price-drop badge** (% drop ≤14d) | ✅ | Shared `price-drop-badge.tsx` (detail + card variants, success tone + TrendingDown). Shown on `/listings/[id]` and every `ListingCard` when `priceDropPercent` is present. Reuses `priceDropPercent`/`priceDroppedAt` (both nil outside the Rails 14-day `PRICE_DROP_WINDOW`) — no contract change; 3 locales; RTL + dark |
| N805 | **Seller response-rate badge** | ✅ | "Usually responds within…" trust row on listing detail seller card + `/sellers/[id]`. Shared `response-rate-badge.tsx`; reuses `:detailed` seller fields (seller page enriched via one listing detail fetch — kept off the `:list` view to avoid browse-feed N+1) |
| V259 | **Saved-by-N social proof** | ✅ | "Saved by N people" meta on `/listings/[id]`, shown only when N>0, HEART icon, styled like the views count. Uses `savesCount` already on the `:detailed` response (serializer `saves_count`, eager-loaded on `#show`) — no contract change. `savesCount` plural keys in en/ps/fa mirror the mobile wording; RTL + dark |
| N071 | Firm / negotiable badge (gates offer) | ✅ | Shared `firm-price-badge.tsx` (muted Badge + Lock, `listing.firmPrice`); shown on `/listings/[id]` beside the price and on every `ListingCard` when `negotiable === false`. `StartConversationButton` hides the Make-offer CTA + offer dialog when firm (`negotiable !== false` = negotiable default, mirrors mobile). Reuses the existing `negotiable` field (served on list/detailed views) — no contract change; 3 locales; RTL + dark |
| C3b | Expiry badge (owner detail) | ✅ | Shared `expiry-badge.tsx` (self-gates on `status === "active"` + ≤7 days). Rendered on both owner surfaces: `/my-listings/[id]` and, via `OwnerListingBar`, the public `/listings/[id]` |
| OWN947 | **Owner action bar on your own listing** | ✅ | `owner-listing-bar.tsx` on `/listings/[id]`. Every buyer control self-hides for the seller (message/phone/save/hide/report) and an active listing never hits the unavailable notice, so a seller opening their own listing had an empty action column with buyer safety tips. Owner-only panel (client, shared `useIsOwner`), primary-tinted so it reads as controls not another info card: `listing.detail.ownListingNotice` + `ExpiryBadge` + a `StatusBadge` **only for `active`** (the page header already badges every non-active status — no double badge), then the listing's **next lifecycle transition** (Publish / Mark as Sold / Renew — resolved by the SHARED `actionsFor()` + `useListingLifecycle()` + `<LifecycleDialogs>` from `account/listing-actions.tsx`, where `actionsFor()` now takes the **listing** (status + `expiresAt` + `expired`) rather than a caller-computed `expired` boolean, so every seller surface derives "has it lapsed?" from the one `listingExpiryState()` rule the badge itself renders — passing the raw server flag let `/my-listings` and the manage screen print a red "Expired" pill beside a primary that was not Renew, so the panel can fix the very status it reports and the copy, the buyer picker and the post-sale `ReviewPromptDialog` can never drift from `/my-listings`; a sale here `router.refresh()`es because this page is server-rendered), Manage → `/my-listings/[id]` (`outline` behind that primary, filled only for a terminal `sold` listing), Edit → `/listings/[id]/edit`, View Chats → `/conversations?listing=[id]` (`secondary`, not `ghost` — a borderless button on a tinted panel reads as static text). Carries no view/save counts — the page's meta row already shows both. `SafetyTips` takes `ownerId` and hides for the owner (buyer guidance), and `UnavailableActions` is wrapped in `HideForOwner` so a sold/reserved listing never tells its own seller to go shop from themselves. **Ownership is resolved during SSR**, not after the browser's session probe: the viewer's id rides in its own cookie beside the (httpOnly) devise tokens (`VIEWER_ID_COOKIE`, written wherever tokens are written, cleared wherever they are cleared), the two `force-dynamic` public pages read it for free and publish it through `ViewerIdProvider`, and `useIsOwner` trusts it while the session is still `loading` — so the seller's first paint is already the owner's page instead of "Message Seller" + save heart + meetup tips flashing past. (An RSC cannot re-probe Rails instead: devise rotates the access-token per request and a Server Component cannot write the rotated one back.) The Chats link carries the free `conversationsCount` as an aria-hidden `Badge` + an sr-only count — **hidden at zero** like every other count badge in the app (Rails always emits `conversations_count`, so the majority of owner views used to read "View Chats 0"), and announced through the manage screen's ICU-plural `listing.conversationsCount` ("1 chat" / "3 chats") so the number is passed raw, not pre-formatted. "Chats" is the parity vocabulary of MOBILE_WEB_PARITY.md §1 in all 3 locales (ps چټونه, fa گفتگوها) — the same word as the nav and `/my-listings/[id]`, not a second name for one destination. The "Active" badge is gated on the shared `listingExpiryState()` (exported from `expiry-badge.tsx`) so an active-but-lapsed listing never reads Active *and* Expired. `aria-labelledby` the notice instead of a duplicate `aria-label`; `min-w-0` on button + label so `truncate` actually applies. The panel's tint is `bg-primary/10 dark:bg-primary/15`: 10% of the dark palette's primary over its near-black background lands *below* `bg-card`, which made the owner's own controls the flattest surface in a column of `bg-card` info cards — the dark override restores the intended hierarchy. No new keys (reuses `listing.detail.ownListingNotice`, `listing.ownerDetail.actions`/`viewConversations`, `listing.conversationsCount`, `common.edit`); no contract change; RTL + dark; 19 Playwright specs in `e2e/listing-detail.spec.ts` (panel contents · the chat count · **no 0 badge on a chat-less listing** · the per-status primary transition · sold-from-the-panel → buyer picker → review prompt · **renew an expired listing from the panel** · **publish a DRAFT from its own public URL** · no Active-beside-Expired · **the chats label is not truncated at 375px in en or ps** · SSR'd with no buyer flash (JS disabled) · buyer controls + safety tips + recovery card + seller rail all hidden · placement above the location/seller cards · no sticky-bar spacer · ps locale + RTL · non-owner unchanged). Two neighbouring alignment/size regressions from the same review are locked in too: the listing page's `ReportButton` gets `-ms-2` so its flag starts exactly where the "Description" heading does instead of 8px inside the column (the trigger keeps its own `px-2`, so the 40px hit box is untouched), and `/sellers/[id]` passes `size="default"` to `ShareButton` so the Share/Report pair are equal-height and both clear 40px — asserted in `e2e/listing-detail.spec.ts` and `e2e/seller.spec.ts`. **DRAFTS ARE COVERED** (an earlier note here claimed the opposite): `GET /listings/:id` resolves through `ListingPolicy::Scope#resolve`, which is `scope.all` — it filters only blocked pairs and admin-removed listings, never `browsable` — so an unpublished listing's public URL renders for its own seller (and the e2e mock serves id 8 as a draft the same way). The panel therefore publishes a draft in place, from `actionsFor` → Publish, and the spec above proves it. The flip side is a **backend** gap, not a web one: because a draft is readable by id, a GUEST on `/listings/8` sees an unpublished item badged "Draft" — `ListingPolicy::Scope` should scope non-owners to `browsable` (a `show?`-level owner check would work too), which is an API card since it is the same hole for mobile Shared-primitive cleanups from the same review: `ui/badge.tsx` gained a solid **`count`** variant (`bg-primary text-primary-foreground`) because the tinted `default` only reached ~3.6:1 behind 12px digits — the panel's waiting-chats pill uses it, and the header's hand-rolled unread pill (`layout/auth-nav.tsx`, which also hardcoded its own 10px type) was dropped onto the same primitive; `shared/expiry-badge.tsx` renders `<Badge variant="destructive"|"warning">` instead of re-implementing that pill at `px-2` beside StatusBadge's `px-2.5`; and `shared/share-button.tsx` now DEFAULTS to the 40px `default` size, so the two pages stopped passing `size="default"` with duplicated comments and the next call site can't regress under the floor. The panel's second button row carries the same `min-w-40 flex-1` as the first, so Edit + Chats wrap to two full-width rows on a phone instead of truncating ("View Cha…"). Dead key `listing.ownerDetail.conversations` (unread by any surface, and its ps wording had drifted from the panel's) deleted from all three catalogs. |
| W628 | Seller "away" banner | ✅ | Shared `away-banner.tsx` ("Seller is away until [date]", `PlaneTakeoff`, primary/info tone). Shown on `/listings/[id]` (buyer, `seller.sellerAwayUntil`), `/sellers/[id]` (public profile, enriched from listing detail), and the seller's own `/profile` (`profile.away.youAreAway`). Guards past dates client-side; reuses `is_away`/`away_until` from the serializer — no contract change. Owner-gated (`HideForOwner`) on **both** public surfaces — the listing page and `/sellers/[id]` — because "this seller is away" is a buyer's reply-time expectation; told to the seller it is the site describing them in the third person (they see `profile.away.youAreAway` instead). 3 locales; RTL + dark |
| L824 | Shareable listing link | ✅ | Shared `share-button.tsx` (Share2 + `common.share`) in the `/listings/[id]` badge row, all statuses. Web Share API when available (localized `listing.share.body` "{title} — {price}" + page URL); otherwise copies `window.location.href` + `common.linkCopied` sonner toast (label flips to `common.copyLink` on desktop). No contract change; 3 locales; RTL + dark |
| B2-STATE | Detail states (skeleton/not-found/sold) | ✅ | |
| SOLDNEXT | **Recovery CTAs on a sold/reserved listing** | ✅ | `unavailable-actions.tsx` replaces the flat grey notice on `/listings/[id]`. Keeps the status sentence (`soldNotice`/`reservedNotice`, falling back to `unavailableNotice` for a draft) and adds the two next steps a guest arriving from Google actually needs: PRIMARY "See similar in {category}" → `/bazaar?category=<slug>&min=<0.7×price>&max=<1.3×price>` and SECONDARY "More from {name}" → `/sellers/[id]`. The band is serialized with the shared `filtersToSearchString`, so the Bazaar sidebar renders category+min+max as active filters and the pill counts 3 — no new param vocabulary; Bazaar only queries `status: "active"`, so the dead listing can't reappear. Exported `priceBand()` drops `min`/`max` for a 0/null/negative price and never emits an inverted range. Each CTA self-omits when its data is missing (no link to nowhere). Reserved adds one muted "a reservation can fall through" line so the unchanged `SaveButton` below still reads as worthwhile. Props-only → **Server Component, zero client JS** on an indexed page. New keys `listing.detail.seeSimilarIn` / `moreFromSellerNamed` / `reservedMayFreeUp` in en/ps/fa (existing notice keys reused verbatim; `moreFromSeller` stays the rail heading). No contract change; RTL (`rtl:-scale-x-100` chevron, logical spacing) + dark; 6 Playwright specs |

## C — Seller: Listings & Lifecycle

| ID | Feature | Web | Migration note |
|---|---|---|---|
| C1 | Create / edit listing (sectioned form) | ✅ | `/listings/new`, `/listings/[id]/edit` |
| C1-PHOTO | Photo upload + reorder | ✅ | ⚠️ web = `<input type=file>`/drag-drop, not native camera |
| C1-CAT | Category picker | ✅ | |
| C1-PROV | Province picker | ✅ | |
| C1-DRAFT | New-listing draft autosave + restore | ⬜ | Persist unsent form to localStorage; restore/discard banner on reopen |
| C2 | My Listings (My Shop) + status tabs | ✅ | `/my-listings` |
| M826 | Per-status tab counts | ❓ | Verify counts on All/Draft/Active/Expired/Reserved/Sold tabs; add if missing |
| C2-* | Lifecycle: publish/reserve/sold/unpublish/delete | ✅ | full lifecycle on `/my-listings/[id]` **and inline on every `/my-listings` card** (TASK-WEB-C2-ACTIONS, mobile parity with `SellerListingCard`). The shared brain lives in `account/listing-actions.tsx` — `LIFECYCLE` copy map + `actionsFor()` + `useListingLifecycle()` (which owns the pending-action state machine, the mutation, toasts, invalidation and the `busy` flag) + `<LifecycleDialogs>` (the confirm-vs-buyer-picker prompt pair) — imported by BOTH the detail view and the card footer, so a surface only supplies its own buttons: no handler, dialog or copy is written twice. The card footer (`account/seller-listing-actions.tsx`) renders one primary Button + a kebab (`ui/dropdown-menu`) with the remaining transitions, Edit and a destructive Delete. The post-sale `ReviewPromptDialog` is owned by the LIST (`seller-listings-view.tsx`), not the card: the `['my-listings']` refetch that follows a sale drops the now-sold card out of a filtered tab, which would unmount a prompt living in the card before the seller could rate anyone. `ListingCard` gained an optional `footer` prop (rendered outside the anchor) and `ListingGrid` a `footerFor` — extended, not forked. No new endpoints, no new i18n keys; RTL (`dir` on the menu, logical spacing) + dark; both controls hold the house **40px tap target** (`min-h-10` on the primary, which stays `h-auto` so a long label wraps instead of clipping; `size-10` on the kebab) — the same floor the Report/Share pair was raised to. 21 Playwright specs in `e2e/my-listings.spec.ts` (per-status primary, kebab contents, each confirm path, buyer picker, buyer-recorded sale → review prompt, in-place refetch with no skeleton flash or reload, failed action → error toast + unchanged card, action row disabled while in flight, the `ps` RTL row + mirrored menu, and the 40px targets fitting a 2-column card at 375px). Every seller surface now imports this one brain — `account/manage-listing-view.tsx`, `account/seller-listing-actions.tsx`, `listing/owner-listing-bar.tsx` and `chat/conversation-thread.tsx` — so `LIFECYCLE`/`actionsFor`/the mutation exist in exactly one file (verified by grep). Design-review follow-ups: the primary is `font-semibold sm:text-sm` (it must not be quieter than the card's meta line — mobile's is 13px/700); a terminal `sold` card renders the trigger full-width WITH the "More options" label instead of a lone unlabeled glyph (mobile parity — `SellerListingCard` does the same, and Edit/Delete live behind it); the primary also carries `listing.detail.actionFor` as its accessible name so seven "Mark as Sold" buttons are told apart by a screen reader; `ListingCardSkeleton`/`ListingGridSkeleton` gained `withFooter` so the pending placeholder reserves the action row's height (no card-height jump + grid reflow when `['my-listings']` lands); the failed-load state is the house `errorTitle`/`errorDescription`/`retry` EmptyState wired to `refetch()`, not a bare "Error"; and `ui/dropdown-menu`'s items are `min-h-10 py-2` app-wide, since on a phone this kebab is the only path to Unpublish/Renew/Edit/Delete |
| C3 | Expiry + **Renew** + Expired tab | ❓ | Renew action is ✅ (parity); verify the **Expired tab** + expiry badge exist, add if missing |
| C-DETAIL | Owner listing detail + manage | ✅ | |
| N802 | 7-day views sparkline analytics | ✅ | views chart on `/my-listings/[id]` |
| C-LCONV | Listing conversations (seller) | ✅ | `/conversations?listing=<id>` |
| W713 | Seller "away mode" (set away-until) | ✅ | Away-mode section in the `/profile/edit` shadcn form: checkbox toggle + native date picker (min=today). On save, away-on + date → `awayUntil = <date>T23:59:59Z`; away-off → `awayUntil: null`; away-on w/ no date → omit (mirrors mobile). Reuses `PUT /users/me` (`awayUntil`) via the existing `/api/me` allow-list — no contract change. Drives the W628 banner. 3 locales; RTL + dark |

## D — Chat

| ID | Feature | Web | Migration note |
|---|---|---|---|
| D1 | Conversations list (thumb/preview/unread) | ✅ | `/conversations` |
| D1-BADGE | Aggregate unread badge | ✅ | |
| K741 | Mark read/unread from the list | ✅ | Per-row kebab item on `/conversations`: "Mark as read" when unread, "Mark as unread" otherwise. Optimistic flip of the row's `unreadCount` (0/1) + rollback on error; invalidates the conversations list and refreshes the user so the header aggregate unread badge stays in sync. Reuses `PUT /conversations/:id/mark_read`·`/mark_unread` (added to the `/api/me` allow-list). Keys mirror mobile `chat.actions.markRead/markUnread/markReadError`; RTL + dark |
| A618 | Archive / unarchive conversation | ✅ | Inbox/Archived segmented toggle on `/conversations` + per-row kebab (Archive in inbox, Unarchive in archived). Optimistic row-removal + rollback on error; invalidates both partitions so the moved row lands on the other tab. Reuses `PUT /conversations/:id/archive`·`/unarchive` (added to `/api/me` allow-list) and `GET /conversations?archived=true`. 3 locales (keys mirror mobile `chat.tabs`/`chat.archive`); RTL + dark |
| D2 | Conversation thread (bubbles, RTL) | ✅ | `/conversations/[id]` |
| D2-READ | Read receipts (double-tick) | ✅ | Gap was real: bubbles rendered no `createdAt`/`readAt`. Every text / attachment / offer / counter / meetup bubble now ends in a meta row — `formatTime(createdAt)` + (on my own messages) a `Check` (sent) / `CheckCheck` (seen) tick titled from `chat.message.sent`·`seen`. Centred system + accepted/declined pills stay bare, mirroring mobile; the deleted tombstone stays bare too (mobile prints a time there — web keeps a retracted message to one quiet line). Thread inserts a day separator between calendar days (`lib/message-days.ts` `dayKey`, local not UTC), labelled `chat.day.today`/`yesterday` else `formatDate`; suppressed while in-thread search is active. No API change (`read_at`/`created_at` already serialized; `mark_read` already wired). The tick flips on the sender's next fetch — Rails broadcasts new messages only. 3 locales · RTL · light/dark · `e2e/chat-receipts.spec.ts` |
| D2-CABLE | Real-time updates (ActionCable) | ✅ | ports directly — web already uses `@rails/actioncable` |
| D3-MEET | Meetup propose + accept/decline | ✅ | |
| D3-OFFER | Offer accept/decline | ✅ | |
| O829 | Counter-offer | ✅ | Seller counters a buyer's offer with a new price from the thread. A "Counter" outline button shows on the buyer's original `offer` bubble only to the seller when it's unanswered; opens a shadcn dialog seeded with the buyer's amount. Sends an `offer_counter` message (same kind mobile uses) reusing the pipe-encoded `amount\|currency\|listedPrice` body over the existing `POST /conversations/:id/messages` — no contract change. Counter bubbles render with the ArrowLeftRight icon + `counterLabel`/`counteredAt` on both sides, and support accept/decline like a normal offer. 3 locales · RTL · light/dark |
| G083 | Offer quick-amount chips | ✅ | Make-offer dialog shows 95/90/85% chips (`shared/offer-quick-chips.tsx`, same `computeChipAmount` as mobile); tap fills the amount without sending, matching chip highlighted, hidden when price is 0/null. Reusable for the in-thread counter composer |
| M482 | In-chat photo/file messages | ✅ | web file upload (photo + document) |
| M913 | **Delete / retract message** (tombstone) | ✅ | Own-message delete behind confirm-dialog; optimistic tombstone + rollback; live flip over ActionCable. Reuses `DELETE /conversations/:id/messages/:id` |
| Q374 | Quick-reply presets | ✅ | Localized canned-phrase chips above the composer on `/conversations/[id]` (shared `quick-replies.tsx`). Buyer/seller phrase set chosen by whether the current user is the listing seller; tapping a chip appends the phrase to the draft (space-separated) and focuses the input — no auto-send. Reuses the mobile `chat.quickReplies.*` keys verbatim in en/ps/fa; hidden on a closed conversation; RTL + dark. No API/contract change |
| C739 | Composer draft persistence (per-convo) | ✅ | Unsent composer text on `/conversations/[id]` persists to a per-conversation localStorage key (`hatiwal.chat.draft:<id>`) via `use-composer-draft.ts` (web port of mobile's `useComposerDraft`): hydrates on open/switch, debounced ~400ms write (flushed on navigate-away), key removed on clear/successful send. Disabled for closed conversations; localStorage failures (private mode) degrade to in-memory. No API/i18n change |
| N803 | **Message search within thread** | ✅ | Header search toggle expands an input; filters loaded messages (text, non-deleted, case-insensitive), highlights matches, shows "X of Y" + loaded-only note. Client-side; RTL + dark |
| F084 | Lifecycle actions from chat header | ❓ | Verify seller can Reserve/Mark-sold from the pinned listing header |
| D2-BLOCK | Block / unblock in thread | ✅ | |
| R483 | Report participant from thread | ✅ | Already shipped: the shared `ReportButton` (icon-only variant) sits in the `/conversations/[id]` header beside the block shield, reporting the other participant as `User`. Verified by `e2e/report.spec.ts` ("Report → block from the conversation thread"); row was stale |
| D2-CLOSED | Closed-conversation state | ❓ | Verify input is disabled with a notice when a conversation is closed |

## E — Saved / Favorites

| ID | Feature | Web | Migration note |
|---|---|---|---|
| E1 | Saved list (grid + infinite scroll) | ✅ | `/saved` |
| E1-TOGGLE | Save/unsave consistency everywhere | ✅ | |
| V836 | Recently viewed (`my/viewed_listings`) | ⬜ | Re-engagement page of listings the buyer viewed |

## F — Profile & Account

| ID | Feature | Web | Migration note |
|---|---|---|---|
| F1 | My profile (stats, entry points) | ✅ | `/profile` |
| F1-MODE | Buyer / seller mode toggle | ✅ | |
| F1-THEME | Theme toggle (light/dark) | ✅ | |
| REP815 | **Own rating + "My reviews" on `/profile`** | ✅ | Your reputation on your own profile (mobile `Profile.tsx` header rating + Reviews quick action → `AllReviews`). `RatingDisplay` renders in the new `UserIdentity` `meta` slot — inside the text column, under your name, at default `sm` so the name stays loudest — wrapped in a thumb-sized (`min-h-10`) link to `#my-reviews` whose accessible name is score + count + destination (no `aria-label`, which would have swallowed the first two). That anchor is the shared `reviews-section.tsx` reused verbatim with `title={t('reviews.myReviewsTitle')}` + `headingSize="sm"` (matching the page's other `h2`s): As-seller / As-buyer toggle, load-more, skeleton, role-specific empty state. **One score per page, everywhere:** `ReviewsSection` never renders a summary — the score always lives in the page header next to the person (`sm` under your name on `/profile`, `lg` hero on `/sellers/[id]`), so `showSummary` is gone rather than passed `false`. `UserIdentity` also gained `nameAs`, making the person's name the `h1` on both profile pages (they had none), and it now drops `meta` when `href` is set — the docblock always promised that, but the code nested an `<a>` in an `<a>`. Data is already in the payload — `UserSerializer` `:me` serves `avgRating`/`reviewCount` — and the list reuses the public `GET /users/:id/reviews` (plain `User.find`, no `publicly_active` gate, so your own id works). No API/contract change. "View my public profile" → `/sellers/[id]`, hidden while `deletionScheduledAt` is set (Rails scopes that page to `User.publicly_active` → would 404). 3 locales (`reviews.emptyAsSeller`/`emptyAsBuyer` added) · RTL (logical props, no left/right) · light+dark · e2e in `e2e/profile.spec.ts` |
| F2 | Edit profile (+ avatar upload) | ✅ | `/profile/edit` |
| F2-LOC | Profile map location | ✅ | ⚠️ browser Geolocation for "use my location" |
| F3 | Public seller profile | ✅ | `/sellers/[id]` |
| F742 | Seller **sold-items showcase** tab | ✅ | Active/Sold segmented control on `/sellers/[id]` (`seller-listings-tabs.tsx`). Active grid is server-rendered; Sold grid lazy-loads via TanStack Query on first Sold-tab open, reusing the guest-accessible `GET /users/:id/sold_listings` (same endpoint mobile uses) — no contract change. Sold cards show a dimmed status badge; empty state when none; 3 locales; RTL + dark |
| V613 | **Verified seller badge** | ✅ | Shared `verified-badge.tsx` (BadgeCheck, `common.verified` title, `text-primary` token) reused via `UserIdentity` at all three sites — listing detail seller card (`/listings/[id]`), public seller profile (`/sellers/[id]`), and conversation thread header. Driven by the existing `verified` boolean on the User payload; no contract change. 3 locales · RTL · dark |
| A356 | "Active recently" label | ⬜ | Privacy-safe last-active label on public profile |
| S392 | Shareable seller-profile link | ✅ | Same shared `share-button.tsx` in the `/sellers/[id]` profile header row (beside Report), body `seller.share.body` ("Check out {name} on Hatiwal", mirrors mobile `profile.share.body`). Web Share API → native sheet; fallback copy-link + toast. 3 locales; RTL + dark |
| B047 | Blocked-users management | ✅ | `/settings/blocked-users` |

## G — Safety, Reporting & Moderation

| ID | Feature | Web | Migration note |
|---|---|---|---|
| G1 | Report listing / user (6 reasons) | ✅ | from detail + seller profile |
| R612 | Report → block follow-up | ✅ | Closed inside the one shared `components/shared/report-button.tsx`, so all three surfaces get it at once (listing detail, `/sellers/[id]`, conversation-thread header). A successful **User** report keeps its success toast, closes the report dialog, then opens the house `ConfirmDialog` (`report.block.*`, destructive) → confirm fires the existing `blockUser()` (`POST /users/:id/block`, already allow-listed) and toasts; a failed block toasts the error and the report still stands (never rolled back). A **Listing** report never prompts. New `alreadyBlocked` prop lets the thread skip the prompt for someone already blocked, and `onBlocked` flips the header shield to Unblock without a refetch. Client-only — no API, no contract change. 3 locales · RTL · dark. E2E: `e2e/report.spec.ts` (7 tests) |
| R739 | "My Reports" status screen | ⬜ | See your submitted reports + their outcome |
| G-WARN | Moderation warnings banner | ✅ | profile warnings banner |
| G-BLOCK | Block / unblock user | ✅ | |

## Cross-cutting / Polish

| ID | Feature | Web | Migration note |
|---|---|---|---|
| P1 | Animation system | ❓ | Web transitions differ (CSS/Framer). Match spirit, not RN Reanimated 1:1 |
| D094 | Contextual empty-state illustrations | ⬜ | Port the per-section empty illustrations (Browse/Saved/Chat/My-Shop) |
| T702 | Persistent data cache | ❓ | Web uses TanStack Query; add a persister only if data-loss on reload is a real issue |
| S0 | Shared component library | ✅ | Web has its own shadcn/ui equivalents |

---

## 📱 Mobile-only — do NOT port 1:1

| ID | Feature | Why / web equivalent |
|---|---|---|
| N801 | Push-notification token registration | `expo-notifications` device tokens. Web = Web Push / service workers (separate rebuild, not a port) |
| C1-PHOTO / M482 | Native camera capture | Web uses `<input type=file>` / drag-drop (the *upload* itself is ✅ on web) |
| B5 / F2-LOC | Device GPS permission | Web uses the browser Geolocation API (different permission model) |
| B2-PHONE | Tap-to-call dialing | Web = `tel:` link / copy number |
| P1-haptics | Haptic feedback | No web equivalent (no-op) |
| L824 / S392 | Native share sheet | Web = Web Share API / copy-link |
| A-store | Secure token storage (Keychain) | Web = httpOnly cookies (already implemented) |
| Q3 / Q5 | iOS/Android platform guards + device test matrix | Native-only concern |
| T701 | Maestro Android E2E CI | Web = Playwright/Cypress instead |

---

## Migration work queue (the ⬜ gaps → `web` board tasks)

Prioritized. Each becomes a `type: web` card on `hatiwal-mobile/docs/SPRINT_BOARD.md` for the software
house. **First batch seeded** = the recent post-audit features with the clearest user value:

**P1 — recent features users already have on mobile**
- ~~`WEB-M913` Delete/retract chat message → tombstone~~ ✅ shipped
- ~~`WEB-D583` Nearest-first sort on Bazaar~~ ✅ shipped
- ~~`WEB-V259` Saved-by-N social proof on listing detail~~ ✅ shipped
- ~~`WEB-N803` In-thread message search~~ ✅ shipped
- ~~`WEB-N804` Price-drop badge~~ ✅ shipped
- `WEB-N805` Seller response-rate badge

**P2 — trust & seller tooling**
- `WEB-C2-THICK` Thicker seller cards on `/my-listings` — views + conversations counts **and** the compact
  "Reserved for {name}" / "Sold to {name}" line: the new inline quick-actions let a seller reserve/sell from
  the card, but the card then says only "Reserved", never for WHOM. Needs a `sale` field on the listing
  payload + `listing.sale.*` in en/ps/fa. Full scope: the "Thicker seller list cards" item in
  `../../docs/MOBILE_WEB_PARITY.md`
- `WEB-C2-SEARCH` Search within My Shop — mobile has it, `seller-listings-view.tsx` has status tabs only
- ~~`WEB-V613` Verified seller badge~~ ✅ shipped · ~~`WEB-F742` Sold-items showcase~~ ✅ shipped · `WEB-A356` Active-recently label
- ~~`WEB-W713`/`WEB-W628` Seller away mode + banner~~ ✅ shipped · `WEB-N071` Firm/negotiable badge · `WEB-C3b` Expiry badge

**P2 — chat depth**
- ~~`WEB-O829` Counter-offer~~ ✅ shipped · ~~`WEB-K741` Mark read/unread from list~~ ✅ shipped · ~~`WEB-A618` Archive conversation~~ ✅ shipped
- ~~`WEB-Q374` Quick-reply presets~~ ✅ shipped · ~~`WEB-C739` Composer draft~~ ✅ shipped · ~~`WEB-R483` Report from thread~~ ✅ shipped

**P3 — discovery & re-engagement**
- `WEB-B931` Most-viewed sort · `WEB-B617` Active-sellers filter · `WEB-B6` Seen indicator
- `WEB-B-VIEW` Grid/list toggle · ~~`WEB-S417` Subcategory drill-down~~ ✅ shipped · `WEB-N612` Saved-search new-match badge
- `WEB-V836` Recently viewed · `WEB-C1-DRAFT` New-listing draft autosave · `WEB-D094` Empty illustrations
- ~~`WEB-R612` Report→block follow-up~~ ✅ shipped · `WEB-R739` My Reports screen · `WEB-W924` First-visit welcome

**Verify-first (❓ — confirm the gap before building):** `C481`, `M826`, `C3` Expired tab,
~~`D2-READ`~~ ✅ shipped (the gap was real), `G083`, `F084`, `D2-CLOSED`.

> Housekeeping: as each `web` task ships, flip its row above from ⬜ to ✅ and update
> `../../docs/MOBILE_WEB_PARITY.md` so the two docs never drift.

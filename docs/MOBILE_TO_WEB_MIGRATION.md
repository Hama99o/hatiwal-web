# Hatiwal — Mobile → Web Feature Migration Catalog

> **What this is:** a complete, line-by-line catalog of **every mobile feature** and its status on the
> **web** client (`hatiwal-web/`, Next.js). It is the source of truth for "what still needs porting
> from the app to the web," and the **work queue the software house pulls `web` tasks from**.
>
> **Read alongside:** [`../../docs/MOBILE_WEB_PARITY.md`](../../docs/MOBILE_WEB_PARITY.md) (page-by-page
> parity map) · [`prompts/web.prompt.md`](prompts/web.prompt.md) (how to build web features the house way)
> · `hatiwal-mobile/docs/BACKLOG.md` (mobile feature detail).

## The honest headline

**Web is NOT starting from zero.** As of the 2026‑06‑21 parity audit the web client already reaches
near-full parity with mobile (browse, auth, seller dashboard, maps, real-time chat, offers, reports,
saved searches, account delete/restore, buyer/seller mode). So this is **not "port 100 net-new
features."** It is a **~110-feature catalog** where most core features are ✅ done, ~10 are 📱 mobile-only,
and the **real migration work is ~30 gaps** — mostly features mobile shipped *after* that audit
(delete-message, nearest sort, saved-by-N, message search, price-drop / response-rate badges, archive /
mark-unread, away mode, counter-offer, etc.).

> ⚠️ **Verify-before-build:** the ✅/⬜ status below is derived from the 12-day-old parity audit + the
> mobile inventory, not a fresh line-by-line read of every web route. A `web` task's **first step** is to
> confirm the gap still exists in `hatiwal-web/` before building (some may have landed since the audit).

### Legend

| Mark | Meaning |
|---|---|
| ✅ | Already built on web — parity confirmed by the audit |
| ⬜ | **GAP — needs porting to web** (the migration work; becomes a `web` board task) |
| 📱 | Mobile-only by nature — do **not** port 1:1 (rebuild with a web equivalent only if it matters) |
| ❓ | Uncertain — web dev must verify against current web code first, then build if missing |
| 🌐 | Web already has this natively / for free (e.g. real URLs, SSR) |

---

## 0 — Onboarding & Bootstrap

| ID | Feature | Web | Migration note |
|---|---|---|---|
| A3 | App bootstrap / splash redirect | ✅ | Web hydrates auth on load; native splash is 📱 N/A |
| A4 | Guest browsing + auth gate (`returnTo`) | ✅ | `GUEST_BROWSE_AND_AUTH_REDIRECT.md` |
| W924 | First-run onboarding welcome carousel | ⬜ | Optional web first-visit welcome (dialog/hero), gated by a cookie/localStorage flag. Low priority |

## A — Authentication

| ID | Feature | Web | Migration note |
|---|---|---|---|
| A1 | Login | ✅ | `/login` |
| A2 | Register | ✅ | `/signup` |
| A-FP | Forgot / reset password | ✅ | `/forgot-password`, `/reset-password` |
| A-SO | Sign out | ✅ | profile + header dropdown |
| A-LANG | Language switcher (en/ps/fa) | ✅ | header locale switcher |

## B — Buyer: Browse & Discover

| ID | Feature | Web | Migration note |
|---|---|---|---|
| B1 | Browse (Bazaar) feed + infinite scroll | ✅ | `/bazaar` |
| B3 | Keyword search | ✅ | |
| B3-HISTORY | Recent-search memory (last 10 terms) | ✅ | One client-only store `src/lib/use-search-history.ts` (localStorage `hatiwal.searchHistory`; trim · min 2 chars · case-insensitive dedupe · newest-first · cap 10 — mobile's `searchHistory.store.ts` rules) rendered by the shared `components/shared/search-history-panel.tsx` from BOTH fields through `components/shared/search-box.tsx`. ONE rule, ONE presentation: each field floats the chips as a dropdown under itself while THAT field is focused and empty — so with the header field and the Bazaar field both on screen at `lg`+ the same history can never show twice, the block never pushes the Bazaar filters down (nor shifts the column when the client-only history hydrates) and Escape/blur always dismisses it. Floating also means the panel owes nothing to its host field's width, and from `md` up it is deliberately allowed to be **wider** than that field (`w-full md:w-[max(100%,24rem)]`, anchored `start-0` so it mirrors in RTL — field-relative at both breakpoints, since a `100vw` cap includes the scrollbar and so overhangs the content box the field sits in): matching the Bazaar's 250px column (or the header bar field wherever the nav squeezes it — measured 379px at 768px) stacked all ten terms one per row with most of them behind a scroll, where 24rem fits two chips to a row and puts the whole capped history on screen; measured at 768/900/1280px in en and ps, the panel stays inside the content box with zero page overflow. Chip labels carry `dir="auto"`, so a Latin term inside the RTL ps/fa panel still elides from its END and keeps the brand word — matching mobile's `numberOfLines={1}`. The Bazaar's own field is `lg`+ only; below that the header's field is the one on screen and it seeds + re-syncs from `?q=` (`popstate` included), so the visible box always carries the active query at every width. At `lg`+ BOTH fields are on screen, and web is the only client with that problem (mobile's Browse has exactly one field) — so whichever field commits a query publishes it through the tiny module store `src/lib/use-browse-query.ts` (`useSyncExternalStore`, `getServerSnapshot` = nothing published yet; the snapshot is the bare term, so re-publishing one nobody has to act on is not news) and every other field adopts it. The URL is still the source of truth but cannot be the channel: the Bazaar commits with `router.replace()` on the SAME pathname, which fires no `popstate` and only reaches `window.location` once the RSC payload lands, so before this the app's most prominent search box could sit there advertising a term the feed was NOT filtered by (refine or clear in the sidebar → the header kept the old query, X and all). The Bazaar publishes from its one filters→URL effect, so every path — settled debounce, Enter, a recent-search chip, "Reset filters", an applied saved search — is covered by construction. A field that is mid-edit DEFERS the news rather than dropping it (a publication is announced once, so a dropped one never returns): it is stashed and adopted the moment the field goes clean without committing anything of its own, which is why erasing half-typed text can never leave the header empty next to a filtered feed. Keyboard: ArrowDown enters the chips, arrows rove (RTL-aware), Escape returns the caret; the input points `aria-controls`/`aria-describedby` at the panel and an `sr-only` hint while it is open (deliberately not a combobox — each chip owns a Remove button). Chip re-runs the search through the existing `?q=` path. No API, no auth, nothing new in the URL — works logged out. 21 Playwright specs in `e2e/search-history.spec.ts` — including both directions of the two-field sync at 1280px (refine + clear in the Bazaar field, and a search made in the header), the panel's RTL anchoring (shared right edge in ps, left in en) and the 250px panel's readability geometry, the cap enforced on the WRITE path (an 11th real search drops the oldest), and the chips' 40px remove target measured at 375px with a long term (the box, not just the aria-label) |
| B3b | Category filter chips + picker | ✅ | |
| B7 | Item-condition filter | ✅ | |
| B-PRICE | Price-range filter | ✅ | |
| B5 | Map location & distance filter | ✅ | Leaflet; ⚠️ GPS uses browser Geolocation, not `expo-location` |
| B-SORT | Sort: newest/oldest/price↑↓ | ✅ | |
| B931 | Sort: **Most viewed** | ⬜ | Add `mostViewed` sort option to `/bazaar` sort control + wire `sort` param |
| D583 | Sort: **Nearest first** | ✅ | Added `nearest` sort option on `/bazaar`; acquires a browser-Geolocation fix (denial/failure → friendly toast + clean fallback to default sort) and sends lat/long with `sort=nearest`. Reuses the existing `GET /listings` (Rails `sort=nearest`). Coords kept ephemeral/client-only (never in the URL) so reload falls back cleanly. 3 locales · RTL · dark |
| B617 | "Active sellers" filter (signed in ≤7d) | ⬜ | Add toggle chip to `/bazaar` filters; param already served |
| C481 | "Filters active (N) · Clear all" pill | ❓ | Verify the redesigned filter sidebar already shows an active-count + clear-all; add if missing |
| B4 | Saved searches / filter history | ✅ | |
| N612 | Saved-search "new matches" badge | ✅ | Badge + mark_seen on the browse saved-searches sidebar |
| B6 | "Seen / already viewed" indicator | ✅ | The card half already existed (`shared/listing-card.tsx` renders the "Seen" pill + `opacity-80` dim off `listing.isViewed`, in both grid and list variants) — it was simply unreachable, because the Bazaar fetched `GET /listings` anonymously and an anonymous payload always says `is_viewed: false`. Closed in the FETCH LAYER, once, by `getListingsAsViewer()` (`lib/api/listings.ts`): same `toParams` mapping and `ListingsResult` shape, but issued through the `/api/me` proxy so the devise tokens are attached and Rails can personalise (`not_hidden_for(current_user)` + `viewed_ids:` → `is_viewed`; verified against the serializer — `field(:is_viewed) { |l, opts| opts[:viewed_ids]... }` lives in `view :list`, fed by the controller's `viewed_listing_ids`, which is an empty Set for a nil `current_user`). One entry added to the proxy allow-list (`["GET", /^listings$/]`). `browse-client.tsx` takes that branch when `useAuth().status === "authed"`, scopes the query key by viewer id so a guest's feed and a buyer's are never one cache entry, and marks the (guest) SSR seed stale for an authed viewer (`initialDataUpdatedAt: 0`) so the personalised refetch starts on mount — the seed is still the LCP paint. A 401/403 — or a 5xx / network throw — falls back to the anonymous feed, so neither an expired session nor an upstream blip costs the buyer the Bazaar, only its personalisation. Same fix delivered the sibling on that payload: hidden ("Not interested") listings really leave the feed. It did NOT change the saved hearts, and cannot: `is_saved` is defined only in `view :detailed`, so the feed has never carried it for anyone, and every feed heart still resolves from `['saved-listings']` (unpressed + `aria-busy` until it lands — never a false outline). Backend follow-up, which also fixes mobile's Browse: `TASK-BE-SAVEDLIST`. **Residual gap:** the SSR-only grids — home's "Recent listings" rail, `/categories/[slug]`, and the listing page's cross-sell rails — are still guest-rendered, and deliberately so: they are Server Components, devise rotates the access token on every request, and an RSC cannot write the rotated cookie back (that is how a valid session gets signed out). Closing those needs a different mechanism (a client island or a server-side rotation-safe read), tracked on the #250 line in `docs/WEB_BACKLOG.md`. Card `TASK-WEB-FEED250`; specs in `e2e/bazaar.spec.ts` |
| B-VIEW | Grid / list view-mode toggle | ⬜ | Persisted grid↔list toggle on `/bazaar` |
| B-STATES | Feed states (skeleton/empty/error) | ✅ | |

## Categories

| ID | Feature | Web | Migration note |
|---|---|---|---|
| C736 | Categories hub (grid + live counts) | ✅ | `/categories` (also SEO landing pages) — one `categories?with_counts=true` call feeds the counts + children; a parent's count **rolls its subcategories up** (matches what `?category_id=` returns); empty categories are de-emphasised and sorted last; skeleton/empty/error states |
| S417 | Subcategory drill-down | ✅ | `CategoryBadge` chips (44px, own count) on each hub card → `/categories/<child-slug>`, overflowing to "+N more"; Bazaar sidebar nests children under the selected parent (`?category=<child-slug>`). **Every** child gets a chip — ranked by stock, never filtered by it: sellers file on the top-level category, so on the real DB all 39 subcategories sit at 0 and a "stocked children only" rule renders no chips at all. An empty child is marked `tone="empty"` (dashed, count in the accessible name) instead of being hidden |

## B2 — Listing Detail (buyer view)

| ID | Feature | Web | Migration note |
|---|---|---|---|
| B2 | Listing detail (increments views) | ✅ | `/listings/[id]` |
| B2-GAL | Image gallery carousel | ✅ | |
| B2-SELLER | Seller card → public profile | ✅ | |
| B2-PHONE | Seller phone reveal | ✅ | ⚠️ web = reveal + `tel:`/copy link, no native dialer |
| B2-MSG | "Message seller" CTA → first message | ✅ | |
| B2-OFFER | Make an offer (quick-amount chips) | ✅ | |
| B2-SAVE | Save-heart (optimistic) | ✅ | |
| B2BAR | **Sticky mobile action bar** (price + Message seller + Save) | ✅ | `listing-action-bar.tsx` — web port of mobile's sticky bottom CTA. Client island, `lg:hidden` **and** gated on `matchMedia("(max-width: 63.999rem)")` — the rem complement of Tailwind's `lg`, so the JS gate and the class can't disagree — so it is never in the desktop DOM (verified: present at 844/1000/1023px, absent at 1024). One `IntersectionObserver` watches two targets and pins the bar only while NEITHER is on screen: `#listing-actions` (don't duplicate a CTA the buyer is already looking at) and `[data-site-footer]` (a fixed bar would cover privacy / delete-account with nothing left to scroll). Carries exactly price + Message + Save: reuses `StartConversationButton` (`layout="bar"` → compact row, **no** offer button — a second 40px control left 98px of box for a 125px label on a 360px phone) + `SaveButton` (`variant="bar"`) verbatim, so conversation/save state is shared with the inline block, never duplicated. Returns null for the seller's own listing (`useIsOwner` — no clash with `OwnerListingBar`) and for non-active listings (the `UnavailableActions` recovery CTAs own that case), and it renders its own height spacer so those three no-bar cases get no dead space above the footer. Slides via `bottom`, and the frosted background is a separate layer — `transform`/`backdrop-filter` would make the fixed bar the containing block for the message dialog. `inert` while hidden. One new key `listing.detail.actionBarLabel` in en/ps/fa; RTL mirrors (price on the start side) + dark; 10 Playwright specs (`e2e/listing-action-bar.spec.ts`).<br>**Independent review (cycle 5):** all 11 specs re-run green (serially — 8 parallel workers against a dev server flake on page loads, not on the bar), `tsc`/eslint clean. One real defect found and fixed: the shared `Dialog` is **not portalled**, so the message/offer dialog opened from the bar is a *descendant* of the bar — anything that unpinned the bar mid-compose (rotating the phone, an iOS scroll-behind that `body{overflow:hidden}` doesn't hold, any reflow that brings `#listing-actions` into view) faded the open dialog to `opacity-0`, marked it `inert` and slid the half-typed message off screen. `StartConversationButton` now reports its dialog state through an optional `onDialogOpenChange`, and the bar holds still (`shown = pinned \|\| dialogOpen`) while it hosts an open dialog, handing control back to the observer on close. Regression spec: "keeps its dialog alive when the bar would otherwise slide away"<br>**Independent review (cycle 6) — 4 defects fixed:** **(1) The bar's heart and the inline heart diverged.** Both `SaveButton`s live for the whole page (the bar is CSS-hidden, never unmounted) and each held its own `override`, so unsaving in one place left the other filled *permanently* and its next tap fired DELETE at an already-unsaved listing. The optimistic flip now lives in ONE cache entry — `['saved-flips', userId]`, written by a `useMutation` `onMutate` with per-listing rollback, `['saved-listings']` invalidated on success and the flip dropped once the refetched list confirms it — so every heart for a listing reads one source (viewer-scoped because logout does not clear the query cache). A write into `['saved-listings']` itself can only express *unsave*: adding needs a whole `Listing`, which the id-only call sites do not have, and a stub would render as a broken card on /saved. Also fixed the pre-existing cause of wrong hearts: `getSavedListings()` fetched page 1 only, so a buyer with >20 saves saw an OUTLINE heart on a listing they had saved — it now pages to the end like `getMyListings()`. **(2) Crossing `lg` mid-compose destroyed the half-typed message** — `if (!compact) return null` unmounted the bar's whole subtree (rotating a tablet, dragging a desktop window across 1024px), and the inline block's button is a different instance with empty state. The shared `<Dialog>` is now **portalled to `<body>`**, so no dialog is ever a descendant of an animated, hidden, conditionally-rendered or `z-40`-capped host; the bar additionally stays mounted while it hosts an open dialog. That retires the "never put `transform`/`backdrop-filter` on the bar" constraint, so the separate frosted layer moved back onto the bar itself. **(3) The dialog-open report could latch true** and pin the bar over the footer forever: `StartConversationButton` reported `open || offerOpen` even after its `isOwner` / `status !== "authed"` early returns had stopped rendering the dialogs (logout mid-compose), so the report is now gated on `dialogsMounted` and cleaned up on unmount. **(4) a11y:** closing the bar's dialog restored focus to a button that was about to be `inert`, dropping the caret at `<body>` so the next Tab restarted at the top of the document — the bar now hands focus to the inline CTA it defers to. 14 specs (3 new: both hearts are one state, survives crossing `lg` mid-compose, focus handoff on close); the 10 dialog-heavy suites re-run for the portal change<br>**Reviews (cycles 7–9) — the bar's three controls stopped guessing, and the price stopped appearing twice.** **(1) Indeterminate ≠ "not saved".** On a cold load the heart's truth is two SEQUENTIAL round-trips away (`/api/auth/session`, then `/api/me/my/saved_listings`), and `initialSaved` cannot bridge it — this page's payload is fetched anonymously, so `isSaved` is `false` even for a listing saved months ago (only `true` is trustworthy there). The pinned heart therefore used to paint the OUTLINE heart labelled "Save listing" for ~1s+ and fire `POST save` on an already-saved listing, so a buyer who wanted to unsave had to tap twice; a failed list fetch claimed "not saved" forever with no toast and no retry. `SaveButton` now models a third state: while `status === "loading"`, the shared list is `isPending`, or it `isError`, the heart is `unknown` — muted fill, `aria-busy`, **no** `aria-pressed` — and a tap there is *queued* and replayed against the resolved value (a failed list is refetched on that tap, and the toast + release only come if the retry fails too). **(2) The CTA did the same thing, louder.** While auth was still resolving `StartConversationButton` rendered its GUEST branch — an `<a href="/login">` labelled "Message Seller", pinned from the first paint — so a signed-in buyer who tapped it was navigated off the listing to a login page they don't need. It now renders a real button in the same unsettled chrome (dimmed, `aria-busy`, `aria-disabled` once a tap is held) and replays the tap into the message dialog when the identity lands. Both controls share one contract: never render byte-identical to the ready state while a tap cannot be honoured, and never assume an answer. **(3) No double price.** The bar used to carry the price from the first paint while the hero price was still on screen — measured at 390x760, "AFN 30,000" twice, 120px apart. A separate `priceAnchorId` (`#listing-price`) now governs the bar's price *only*: it is added to the same observer's targets and the price renders only while the hero one is off screen. Deliberately not part of the hide set — the hero price ends at y=651 and `#listing-actions` starts at y=1201, a 550px gap inside a 760px viewport, so gating the whole bar on the price would leave no scroll position where it ever appears, deleting the feature (mobile makes the same trade: its sticky bar carries no price at all). **(4) Tablet slab.** `flex-1` alone stretched the primary to a 566x40 banner between 640px and 1023px; capped with `sm:max-w-xs md:max-w-sm` + logical `sm:ms-auto` so it groups against the heart as a toolbar. **(5) Spacer drift.** The hand-written `calc(4rem + safe-area)` was 1px short of the real 65px bar and would desync silently the next time the row grew — the bar now publishes its measured height via `ResizeObserver` and the spacer consumes it (the class is only the first-paint fallback). Not done, and deliberately: mobile keeps its bar on reserved/sold with a muted notice, but this card's acceptance requires the bar to be ABSENT there, and the inline `UnavailableActions` + hero `StatusBadge` already carry the status — a parity idea for a follow-up card, not a defect. **19 specs** (`e2e/listing-action-bar.spec.ts` — see the correction in cycle 10 below), 5 new: the two-prices invariant at every scroll depth, the tablet cap, the queued unsave, the queued tap during auth bootstrap, and the CTA that must not bounce a signed-in buyer to /login (all mutation-checked against the pre-fix code). The two-prices spec was also de-flaked: an IntersectionObserver gate cannot be frame-exact, so the price/hero biconditional is asserted once settled instead of on the raw first sample, which failed 2 runs in 3<br>**Review (cycle 10) — the bar stopped being dead on arrival for the traffic that matters most, and its "unsettled" contract became one implementation.** **(1) The seller CTA was dead in the server HTML, potentially forever.** (Precisely: the **inline** `StartConversationButton` at `page.tsx`, which is the one that ships in the SSR payload and the one the `expect(html).toContain('href="/en/login"')` spec measures. The *pinned* copy is never in the server HTML at all — `listing-action-bar.tsx` initialises `compact = useState(false)` and only sets it from `matchMedia` inside an effect, so `if (!compact && !dialogOpen) return null` makes the whole bar client-only. Same component, same defect, so the fix covers both.) The cycle 7-9 fix above replaced the guest branch during `loading` with a dimmed `aria-busy` button, so a cookie-less request (search traffic — the bulk of listing-detail visits) got `<button aria-busy>` and the page contained **zero** `/login` hrefs: a tap at first paint was simply lost, and the worst case was unbounded, not the ~1s that was claimed — `AuthProvider.refresh()` had no timeout and its 1500ms backoff only covered REJECTIONS, so a request that merely hangs (dead mobile network, captive portal) left the primary action dimmed and dead with no path to sign in. The server already knew: this page is `force-dynamic` and `readViewerIdFromCookies()` is itself a session probe. `owner-gate.tsx` now also exports **`useServerViewerId()`** (context widened to `number | null | undefined`, so "no provider" is distinguishable from "guest"), and both the CTA and the heart pick the REAL branch during `loading`: hint `null` → the guest UI, in the SSR HTML, working before hydration and with JS off; hint a number → never a /login link; no hint (an ISR page) → the guest markup is kept and intercepted, so the same element degrades to a working link and a hydrated tap is still queued. `refresh()` also got an `AbortSignal.timeout(8_000)` so a hang becomes a rejection the retry loop can end. **(2) a11y/contrast.** `opacity-70` on the primary CTA dimmed its LABEL: white `--primary-foreground` on `--primary` at 70% over the bar's `bg-background/95` measures ~3.1:1 against the 4.5:1 AA floor for 14px `text-sm font-medium` (light mode only; the icon-only heart is fine, judged against the 3:1 non-text rule). The unsettled cue for a labelled control is now a `secondary` variant plus a spinner once a tap is held — no dimming of text. **(3) One implementation of "this control cannot honour your tap yet".** New `src/lib/unsettled.ts` (`unsettledProps({unknown, busy, queued, tone})` + `useQueuedTap(pending, run)`) replaces the hand-copied `opacity-70` / `animate-pulse` / `aria-busy`+`aria-disabled` / queue-and-replay effect that had been duplicated across `save-button.tsx` and `start-conversation-button.tsx` — and it is what made fixing the contrast bug a one-line change in one place. **A third consumer turned up while verifying: `ReportButton` had the same defect** — `status === "loading"` was read as "not authed", so a signed-in person who tapped Report during bootstrap was pushed to /login, off the page they were reporting. It now holds the tap the same way (regression spec in `report.spec.ts`). **(4) Focus was dropped on every guest load.** The `loading` and `guest` branches rendered different root elements (`div` > `Button` vs the `<a>` of `Button asChild`), so when the probe resolved React unmounted instead of reconciling and a keyboard user's caret fell to `<body>`. All three branches now share one wrapper shape. **(5) Tablet cap moved from the button to the ROW** (`sm:max-w-md` on `mx-auto flex max-w-6xl`): capping the button fixed the 566x40 slab but applied in the price-less state too, leaving ~300px of empty frosted strip before the CTA at 768px. The row cap is identical in both price states and centred, so it reads the same in ps/fa — both states are now measured. **(6) Touch targets.** The bar is `lg:hidden`, i.e. the one touch-only surface on the site, and its controls were 40px against the ≥44px floor in `docs/DESIGN_SYSTEM.md`; the CTA is `h-11` and the `bar` heart `size-11` (the spacer is ResizeObserver-measured, so it followed on its own). **(7) Shared primitive: `ui/textarea.tsx`** — five call sites had hand-copied `Input`'s class base and drifted (this component and `report-button` had dropped `placeholder:text-muted-foreground`; all five had dropped the focus-ring offset and the `disabled:` treatment), so all five now use one primitive. **(8) Parity of vocabulary.** The pinned CTA said "Message Seller" while the app's same button says "Contact Seller": it now uses **`listing.detail.contactSeller`**, the same key mobile's `ListingDetail.tsx` uses (parity rule 2), and the dialog it opens keeps `chat.startConversation.title` — the analogue of mobile's composer sheet, which is where mobile uses `messageSeller`. The then-unreferenced `listing.detail.messageSeller` was deleted from all 3 catalogs, and the suite now reads its expected labels FROM the catalogs so a copy change cannot leave them stale. **(9) Spec-count correction and two weak assertions.** The "19 specs" above counted the runner's output, which includes `auth.setup.ts`'s 2 fixtures; the file now holds **20 tests** (18 mobile/guest + 1 breakpoint + 1 desktop). The overflow spec measured `scrollWidth > clientWidth` at scrollY=0, where the two-prices spec proves the bar carries NO price — so the crowded price + CTA + heart row it exists for was untested at every width; it now scrolls below the inline block first and runs `en` at 360/390/430 plus `ps` and `fa` at 360 (ps's `contactSeller` is 18 characters against 14 in en — the real worst case). And the "no navigation" guard after the held tap was vacuous (`toHaveURL` polls and passes on its first sample, so a bounce a tick later slipped through); it now counts main-frame navigations across the click. Full suite re-run: 248 tests, green (serially; 4 parallel workers flake on unrelated page loads, as recorded in cycle 5). `tsc` + eslint clean, `npm run build` clean<br>**Review (cycle 13) — the session probe stopped being able to sign a valid session out, and "unsettled" stopped costing contrast.** **(1) The probe is never aborted.** `AbortSignal.timeout(8_000)` on `/api/auth/session` was two bugs at once. It is not universal — absent on Safari <16 (every iPhone on iOS 15), Chrome/WebView <103, Firefox <100, and Next does not polyfill it — and the expression throws `TypeError` **synchronously**, before `fetch`, so on those browsers all four attempts failed without a single request and every signed-in visitor was shown as a guest: strictly worse than the hang it was added for. Worse, that endpoint **rotates** the devise access-token and the rotated value only reaches the browser in its own `Set-Cookie`, so an abort after Rails had rotated discarded a token Rails had already replaced; the retry 1.5s later presented the stale one, outside devise_token_auth's 5s batch buffer, so Rails answered 401 and the route ran `clearTokenCookies` — the probe would have **manufactured** the one 401 this code is careful never to cause, signing a valid session out on exactly the slow network it was meant to help. The request now runs to completion, and the hang is bounded **presentationally** instead: `AuthProvider` exposes `probeTimedOut` (8s, `status`/`user` untouched, the probe still in flight and still authoritative), and the ONE control that can genuinely be left with no answer — `SaveButton` on an ISR page, where no `ViewerIdProvider` exists to read cookies — falls back to its guest state, whose tap goes to /login. Never applied where the server DID answer: demoting a hint-authed viewer to guest markup is the defect cycle 10 fixed. **(2) `opacity-70` is gone from the hearts.** `tone: "icon"` was licensed by WCAG's 3:1 non-text rule, which it does not clear against this project's own tokens over `bg-background/95`: the `unknown` heart (muted glyph **plus** the dim, two cues stacked) is 2.64:1 light, and the optimistically-filled `fill-destructive` heart 2.56:1 light / 2.47:1 dark, with `animate-pulse` dipping further — the ~6.5:1 the module claimed holds only for a `text-foreground` glyph, i.e. exactly the glyph the unknown branch replaces. Undimmed the colour swap alone clears it (4.52:1 / 3.62:1), so the colour is the cue; that also retired the same dim on the `detail` variant, which is a **labelled** button and so was failing the 4.5:1 text floor too. **(3) One decision for labelled controls, made in the module.** `unsettledProps({tone:"text"})` returned an empty className and silently delegated rule 1 to each caller, and the two callers disagreed — the CTA paid with a `secondary` variant while `ReportButton` rendered pixel-identical to ready. The variant was the wrong half of that disagreement: it dropped fill-vs-bar contrast from 4.97:1 to **1.19:1 light / 1.35:1 dark**, so on every signed-in cold load the pinned primary action rendered as bare text with no button shape and then popped to blue — it erased the affordance the bar exists for, on the one surface whose job is "price prominent, one loud action". Both text callers now keep their ready chrome, `aria-busy` carries the state, and the spinner on a held tap is the only visual change; the module documents that as the decision rather than leaving it to the third caller. **(4) Dead code removed:** the CTA's `serverAuthed ? … : <ISR fallback>` else-branch was unreachable (both render sites are inside `ViewerIdProvider`) and was also the one branch that rendered a different element type, i.e. the focus-drop defect item (4) of cycle 10 fixed, waiting to come back. **(5) Test hygiene:** `hold()` moved to a shared `e2e/route-gate.ts` (two suites need the same gate); `report.spec.ts`, `listing-detail.spec.ts` (6 sites) and `message-seller.spec.ts` now read `listing.detail.contactSeller` from the catalog like `listing-action-bar.spec.ts` does, and the composer placeholder too, so the claim that "the suite reads its labels from the catalogs" is finally true of every suite that asserts them; the docblock the previous pass left stranded above `expectSettledCta` is back on `gotoSellerAuthed`. **4 new regression specs** (23 in `listing-action-bar.spec.ts` + 1 in `saved.spec.ts`), each mutation-checked against the pre-fix code: the CTA keeps its primary fill for the whole wait; a 10s held probe is never re-issued (measured from the first probe, so dev Strict Mode's double mount can't flake it) and never demotes a hint-authed viewer; an unsettled heart is `opacity: 1` and signals by colour; and an ISR-page heart stops waiting on a probe that never answers and offers /login. Suites re-run: `listing-action-bar` 25/25, `saved` 6/6, `report` + `message-seller` + `listing-detail` 63/64 — the one failure is the `owner-listing-bar` ps spec, which passes in isolation and belongs to another session's in-flight refactor of that component. `tsc` + eslint clean. Four pre-existing follow-ups filed rather than left in review text: FlowApp #246 (settle controls on ISR pages — hearts + auth-nav), #248 (report dialog 44px rows + a real `ui/radio-group`), #249 (dead `animate-in` classes), #250 (anonymous rail/feed fetches resurface hidden listings), all also in `docs/WEB_BACKLOG.md`. **Cycle 17 (design + code review fixes):** the held-tap cue is now a GLYPH and nothing else. Cycle 16's `animate-pulse` was the same mistake as the `opacity-70` it replaced, one step worse — it troughs at `opacity: .5`, so the muted heart bottomed out at 1.90:1 against WCAG's 3:1 non-text floor (and `save-button.tsx` passed `tone: "icon"` unconditionally, pulsing the LABEL of the full-width `detail` Save button to 3.38:1) — while `motion-reduce:animate-none` left a `prefers-reduced-motion` viewer with NO feedback at all: tap, nothing, tap again, swallowed by the `if (busy) return` guard. So `unsettledProps` owns no class names now (the `tone` argument is gone; it returns ARIA only) and each caller swaps its own mark while a tap is held — `Heart`/`MessageCircle`/`Flag` → `Loader2`, the same box, no contrast cost, visible with animations off. The compact bar row keeps its one documented exception (no idle icon: the +24px is what makes the primary label fit at 360px) and earns the spinner on a held tap. Also: every gated buyer action goes through one `loginHref` helper (`src/components/auth/login-href.ts`), so the pinned CTA's guest href in the server HTML is `/en/login?next=%2Flistings%2F2` — a bare `/login` used to land the buyer on /profile with the listing they were reading gone — and the session probe's UI budget is DERIVED from its own backoff ladder (12s) instead of a flat 8s that fired while a fast-rejecting probe was still retrying. **2 new regression specs:** a held tap swaps the glyph with `prefers-reduced-motion` emulated and `opacity` still 1; and the signed-in slow-probe case on an ISR page — the tap is HELD (no save request, no navigation) while an answer is still possible, and the last-resort fallback carries `?next=` so it can be walked back. The two guest-CTA assertions cycle 16 left asserting a bare `/login` were corrected to pin the recovering href. Suites re-run serially: `listing-action-bar` 24/24, `saved` 5/5, `report` + `message-seller` 11/11, `listing-detail` 55/57 (both failures pass in isolation and belong to another session's live edits to `listings/[id]/page.tsx`); `tsc --noEmit`, `eslint src e2e` and `next build` all clean |
| G274 | Listing map snippet | ✅ | Leaflet |
| B173 | Similar-listings rail | ✅ | Bottom of `/listings/[id]` (no separate route) — `getSimilarListings(id)` → the dedicated `GET /listings/:id/similar` endpoint mobile uses (`Listing.similar_to`: browsable only, same category **and its children**, source excluded, newest first, max 8), rendered through the shared `listing-rail.tsx`; heading `listing.detail.similarListings`, "view all" → `/categories/[slug]` (see M547) |
| M547 | **"More from this Seller" rail** | ✅ | Second cross-sell rail on `/listings/[id]`, above Similar listings: `getListings({ userId: seller.id, status: 'active' })` (one extra request, skipped when there is no seller), current listing filtered out, capped at 4, trailing "view all" → `/sellers/[id]`. **Buyer-only** — wrapped in the new `components/auth/owner-gate.tsx` (`HideForOwner` + `useIsOwner`), mirroring mobile's `!isOwnListing` gate (`ListingDetail.tsx`): a seller must not be cross-sold their own stock under buyer copy with a "view all" to their own profile. The gate is the only client code; the rail stays a Server Component. `owner-gate.tsx` is now THE single owner test, reused by `SaveButton`, `HideListingButton`, `SafetyTips` and `OwnerListingBar` (the same 3-clause check was written out in each). On a sold/reserved listing the rail drops its own "view all": `UnavailableActions` already owns a prominent, name-carrying CTA to that profile (one destination, one CTA). Every titled listing section on the site now renders through shared `listing-rail.tsx` — both detail rails **and the home page's "Recent listings"** (new `size="lg"` typography variant + `priorityCount` + `empty` slot for its `EmptyState`); it renders nothing when empty and has no `empty`, so a rail can be dropped in unconditionally. This also fixed the similar rail's mislabeled heading (`home.recent` → `listing.detail.similarListings`). Both rails cap at 4 cards and `listing-rail.tsx` renders the `sm` size on the new `rail` column tracks (`grid-cols-2 md:grid-cols-4`) — 4 divides evenly into 2 and 4, so a rail never wraps a lone orphan card or leaves a hole in its last row (the page tracks' `sm:grid-cols-3`/`xl:grid-cols-5` did both); the rail's "view all" is a default-size (40px) Button, not `sm` (36px). Key `listing.detail.moreFromSeller` mirrors mobile; 3 locales; RTL (`rtl:-scale-x-100` arrow, logical spacing) + dark; 14 Playwright specs in `e2e/listing-detail.spec.ts` (rail contents · current listing excluded · ordering · dedupe · divider · a11y names · locale prefix in ps · owner-hidden + guest/other-seller controls · single seller CTA on sold/reserved) + a home-page assertion locking the shared rail in.<br>**Cycle 4:** both rails moved into one async Server Component, `components/listing/cross-sell-rails.tsx`, wrapped by the page in `<Suspense>` — the photo/price/seller column no longer waits on two cross-sell requests, and `CrossSellRailsSkeleton` (heading bar + 4 card skeletons on the same `rail` tracks) reserves the space while they stream. Similarity now comes from the dedicated endpoint (B173) instead of a client-side `categoryId` approximation, so web and mobile answer "what is similar to this?" with the same SQL; the seller fetch dropped from `pageSize: 12` to `RAIL_SIZE + 1` (the only reason to fetch past the cap is that this listing may come back in the page). The two rails are deduplicated through a `Set` of already-shown ids — the seller rail runs first and wins, so a seller's other listing in this same category is never offered twice under two headings (and if it was the similar rail's only candidate, that rail correctly doesn't render). A `Separator` marks the boundary between the two grids, inside the owner gate so it can't dangle. The similar rail got the symmetric "view all" → `/categories/[slug]` (active listings only — on sold/reserved `UnavailableActions` owns both CTAs). A11y: each rail is a named `region` (`aria-label={title}`) and "View all" carries the section title `sr-only` **after** the visible words (WCAG 2.5.3), so two adjacent rails aren't two identical links. `ListingCard` gained `tracks` (`CARD_IMAGE_SIZES`, typed by the same key as the grid's `GRID_COLUMNS`) so a rail card's `sizes` describes the rail's 2→4 layout, not the feed's 2→5. Backend (both clients benefit): `Listing.similar_to` now expands through `by_category` (self_and_children) so an item filed on a parent category still gets a rail, and the `similar` action applies `not_hidden_for(current_user)` so the rail honours "Not interested" — model + request specs added. **Known gap:** on web the rails are fetched anonymously (public payloads never carry the devise token — see `lib/api/client.ts`), so a signed-in buyer's hidden listings can still appear there; the browse feed and category hubs share that gap, and closing it belongs in the fetch layer once for all of them |
| B7b | Condition badge | ✅ | |
| N804 | **Price-drop badge** (% drop ≤14d) | ✅ | Shared `price-drop-badge.tsx` (detail + card variants, success tone + TrendingDown). Shown on `/listings/[id]` and every `ListingCard` when `priceDropPercent` is present. Reuses `priceDropPercent`/`priceDroppedAt` (both nil outside the Rails 14-day `PRICE_DROP_WINDOW`) — no contract change; 3 locales; RTL + dark |
| N805 | **Seller response-rate badge** | ✅ | "Usually responds within…" trust row on listing detail seller card + `/sellers/[id]`. Shared `response-rate-badge.tsx`; reuses `:detailed` seller fields (seller page enriched via one listing detail fetch — kept off the `:list` view to avoid browse-feed N+1) |
| V259 | **Saved-by-N social proof** | ✅ | "Saved by N people" meta on `/listings/[id]`, shown only when N>0, HEART icon, styled like the views count. Uses `savesCount` already on the `:detailed` response (serializer `saves_count`, eager-loaded on `#show`) — no contract change. `savesCount` plural keys in en/ps/fa mirror the mobile wording; RTL + dark |
| N071 | Firm / negotiable badge (gates offer) | ✅ | Shared `firm-price-badge.tsx` (muted Badge + Lock, `listing.firmPrice`); shown on `/listings/[id]` beside the price and on every `ListingCard` when `negotiable === false`. `StartConversationButton` hides the Make-offer CTA + offer dialog when firm (`negotiable !== false` = negotiable default, mirrors mobile). Reuses the existing `negotiable` field (served on list/detailed views) — no contract change; 3 locales; RTL + dark |
| C3b | Expiry badge (owner detail) | ✅ | Shared `expiry-badge.tsx` (self-gates on `status === "active"` + ≤7 days). Rendered on both owner surfaces: `/my-listings/[id]` and, via `OwnerListingBar`, the public `/listings/[id]` |
| OWN947 | **Owner action bar on your own listing** | ✅ | `owner-listing-bar.tsx` on `/listings/[id]`. Every buyer control self-hides for the seller (message/phone/save/hide/report) and an active listing never hits the unavailable notice, so a seller opening their own listing had an empty action column with buyer safety tips. Owner-only panel (client, shared `useIsOwner`), primary-tinted so it reads as controls not another info card: `listing.detail.ownListingNotice` + `ExpiryBadge` + a `StatusBadge` **only for `active`** (the page header already badges every non-active status — no double badge), then the listing's **next lifecycle transition** (Publish / Mark as Sold / Renew — resolved by the SHARED `actionsFor()` + `useListingLifecycle()` + `<LifecycleDialogs>` from `account/listing-actions.tsx`, where `actionsFor()` now takes the **listing** (status + `expiresAt` + `expired`) rather than a caller-computed `expired` boolean, so every seller surface derives "has it lapsed?" from the one `listingExpiryState()` rule the badge itself renders — passing the raw server flag let `/my-listings` and the manage screen print a red "Expired" pill beside a primary that was not Renew, so the panel can fix the very status it reports and the copy, the buyer picker and the post-sale `ReviewPromptDialog` can never drift from `/my-listings`; a sale here `router.refresh()`es because this page is server-rendered), Manage → `/my-listings/[id]` (`outline` behind that primary, filled only for a terminal `sold` listing), Edit → `/listings/[id]/edit`, View Chats → `/conversations?listing=[id]` (`secondary`, not `ghost` — a borderless button on a tinted panel reads as static text). Carries no view/save counts — the page's meta row already shows both. `SafetyTips` takes `ownerId` and hides for the owner (buyer guidance), and `UnavailableActions` is wrapped in `HideForOwner` so a sold/reserved listing never tells its own seller to go shop from themselves. **Ownership is resolved during SSR**, not after the browser's session probe: the viewer's id rides in its own cookie beside the (httpOnly) devise tokens (`VIEWER_ID_COOKIE`, written wherever tokens are written, cleared wherever they are cleared), the two `force-dynamic` public pages read it for free and publish it through `ViewerIdProvider`, and `useIsOwner` trusts it while the session is still `loading` — so the seller's first paint is already the owner's page instead of "Message Seller" + save heart + meetup tips flashing past. (An RSC cannot re-probe Rails instead: devise rotates the access-token per request and a Server Component cannot write the rotated one back.) The Chats link carries the free `conversationsCount` as an aria-hidden `Badge` + an sr-only count — **hidden at zero** like every other count badge in the app (Rails always emits `conversations_count`, so the majority of owner views used to read "View Chats 0"), and announced through the manage screen's ICU-plural `listing.conversationsCount` ("1 chat" / "3 chats") so the number is passed raw, not pre-formatted. "Chats" is the parity vocabulary of MOBILE_WEB_PARITY.md §1 in all 3 locales (ps چټونه, fa گفتگوها) — the same word as the nav and `/my-listings/[id]`, not a second name for one destination. The "Active" badge is gated on the shared `listingExpiryState()` (exported from `expiry-badge.tsx`) so an active-but-lapsed listing never reads Active *and* Expired. `aria-labelledby` the notice instead of a duplicate `aria-label`; `min-w-0` on button + label so `truncate` actually applies. The panel's tint is `bg-primary/10 dark:bg-primary/15`: 10% of the dark palette's primary over its near-black background lands *below* `bg-card`, which made the owner's own controls the flattest surface in a column of `bg-card` info cards — the dark override restores the intended hierarchy. No new keys (reuses `listing.detail.ownListingNotice`, `listing.ownerDetail.actions`/`viewConversations`, `listing.conversationsCount`, `common.edit`); no contract change; RTL + dark; 19 Playwright specs in `e2e/listing-detail.spec.ts` (panel contents · the chat count · **no 0 badge on a chat-less listing** · the per-status primary transition · sold-from-the-panel → buyer picker → review prompt · **renew an expired listing from the panel** · **publish a DRAFT from its own public URL** · no Active-beside-Expired · **the chats label is not truncated at 375px in en or ps** · SSR'd with no buyer flash (JS disabled) · buyer controls + safety tips + recovery card + seller rail all hidden · placement above the location/seller cards · no sticky-bar spacer · ps locale + RTL · non-owner unchanged). Two neighbouring alignment/size regressions from the same review are locked in too: the listing page's `ReportButton` gets `-ms-2` so its flag starts exactly where the "Description" heading does instead of 8px inside the column (the trigger keeps its own `px-2`, so the 40px hit box is untouched), and the `ShareButton` on `/sellers/[id]` renders at the 40px `default` size (its own default now — see below) so the Share/Report pair are equal-height and both clear the floor — asserted in `e2e/listing-detail.spec.ts` and `e2e/seller.spec.ts`. **DRAFTS ARE COVERED** (an earlier note here claimed the opposite): `GET /listings/:id` resolves through `ListingPolicy::Scope#resolve`, which is `scope.all` — it filters only blocked pairs and admin-removed listings, never `browsable` — so an unpublished listing's public URL renders for its own seller (and the e2e mock serves id 8 as a draft the same way). The panel therefore publishes a draft in place, from `actionsFor` → Publish, and the spec above proves it. The flip side is a **backend** gap, not a web one: because a draft is readable by id, a GUEST on `/listings/8` sees an unpublished item badged "Draft" — `ListingPolicy::Scope` should scope non-owners to `browsable` (a `show?`-level owner check would work too). That is an API fix, not a web one — the same hole is open to mobile, which reads the identical endpoint — so it is tracked as its own backend card (**board card 242, TASK-API-DRAFT404**, Backlog); owner readability must survive it, because that is exactly what lets this panel publish a draft in place. Shared-primitive cleanups from the same review: `ui/badge.tsx` gained a solid **`count`** variant (`bg-primary text-primary-foreground`) because the tinted `default` only reached ~3.6:1 behind 12px digits — the panel's waiting-chats pill uses it, and the header's hand-rolled unread pill (`layout/auth-nav.tsx`, which also hardcoded its own 10px type) was dropped onto the same primitive; `shared/expiry-badge.tsx` renders `<Badge variant="destructive"|"warning">` instead of re-implementing that pill at `px-2` beside StatusBadge's `px-2.5`; and `shared/share-button.tsx` now DEFAULTS to the 40px `default` size, so the two pages stopped passing `size="default"` with duplicated comments and the next call site can't regress under the floor. The panel's second button row carries the same `min-w-40 flex-1` as the first, so Edit + Chats wrap to two full-width rows on a phone instead of truncating ("View Cha…") — **superseded in cycle 17**, which demoted Edit out of the stack entirely. Dead key `listing.ownerDetail.conversations` (unread by any surface, and its ps wording had drifted from the panel's) deleted from all three catalogs.<br>**Review (cycle 2) — 6 findings, all fixed.** **(1) The fa Chats label was still being eaten.** `min-w-40 flex-1` moved the clipping window rather than closing it: the row splits 2-up as soon as the panel's inner width passes 328px (viewport >= ~392px, ~150px a cell) while "مشاهده گفتگوها" needs ~184px of button, so fa rendered "مشاهده گفت…" from ~392px to ~480px — and the old spec only measured en/ps at 375px, where the row is still stacked 1-up and nothing can clip. Every button in the panel now carries the house treatment from `account/seller-listing-actions.tsx` (`h-auto min-h-10 whitespace-normal`, one `ROW_BUTTON` constant) and no label truncates: a label too wide for its box wraps and grows the button instead of disappearing. The spec became three (375 · 412 · 430) x three locales, measures BOTH axes on the label AND on its button (a wrapped line cut off by a fixed height is the same defect one dimension over) and re-checks the 40px floor; mutation-verified — restoring `truncate` fails fa at 412 and 430 and passes at 375, exactly the reviewer's measurement. The four-stacked-buttons shape at <=375px is now a documented deliberate call (pairing at every width would put the primary in a half-width box on the narrowest phones). **(2) The `count` badge failed AA in dark.** Solid `--primary` is 4.9:1 in light but 3.6:1 in dark against white, and these pills are 12px, i.e. the 4.5:1 floor applies — the variant that was introduced to escape a ~3.6:1 tint landed on ~3.6:1 again for everyone reading at night. New palette token **`--primary-strong`** (`hsl(221 83% 45%)` light, same hue as each theme's primary) fills it at ~6.7:1 with white text. (The dark value shipped here as `hsl(217 91% 42%)` and was **later inverted** — see cycle 17 at the end of this row: darkening won the digits but cost the pill its salience.) Opaque on purpose: the reviewer's `dark:bg-primary/80` measures ~4.7-5.1:1 but only against whatever the pill sits on, and this pill moves (header, thumbnail, tinted panel). A spec now reads the pill's computed colors in light AND dark, asserts the fill is opaque and computes the WCAG ratio, so the next palette tweak cannot silently re-break it. **(3) The panel's loudest control was the irreversible one, and it diverged from mobile.** `actionsFor()` returned `sold` as the primary for a plain active listing; mobile's shared hook (`useListingLifecycle.ts` `primaryAction`) returns **reserve** and keeps sold in the overflow — and `sold` is terminal here (web still has no duplicate/relist), so the filled button at the top of the seller's own public page was the one with no path back. Active now resolves to `{ primary: "reserve", secondary: ["sold", "unpublish", "renew"] }`, the doc comment spells out the whole status->action table against the mobile hook it mirrors, and the /my-listings + manage + listing-detail specs moved with it (the review-prompt flow now runs through the kebab's Mark as Sold on an active card, and from the panel on the RESERVED listing whose primary it is). **(4) The panel warned about an expiry it could not act on.** For a not-yet-lapsed listing Renew lives in `secondary`, which the panel discards, so the amber "Expires in 3 days" pill stated urgency whose fix was a navigation away — the same gap that justified wiring Renew for the already-expired case. Renew now joins the action row while `listingExpiryState()` says `soon`, WITHOUT displacing the status's own next step, and it is gated on `secondary.includes("renew")` so the shared brain still decides what is legal. Two specs (renew from an expiring listing; no Renew button when there is nothing to renew) plus a new mock fixture — seller 1's listing 11, 3 days out, computed from `Date.now()` so it can never drift into "expired", and kept out of the public feed and My Shop like `EXPIRED_MINE`. **(5) Three count pills, three digit systems.** The header's unread badge and the inbox row's per-thread badge both printed `count > 9 ? "9+" : count` RAW, so on /ps and /fa they showed a Latin "2" beside Arabic-Indic digits everywhere else on the same page — the exact failure mode `e2e/i18n-digits.spec.ts` exists to fence — and each set its own type scale (10px / 11px). All three now render through one shared **`components/shared/count-badge.tsx`**: hides at zero, `formatNumber` digits, a localized cap via the new `common.countOverflow` key, the AA-safe `count` fill, and an optional sr-only sentence (the inbox row's bare "2" became "2 unread messages" via the new `chat.unreadCount`). Three specs added to `i18n-digits.spec.ts` (header pill · a count past the cap, which must read "۹+" with the sign coming from the catalog · the panel's pill). **(6) `ui/card.tsx` was dead code while three surfaces hand-rolled it.** The panel and the listing page's location + seller cards each re-declared `rounded-lg border bg-card`; all three now render `<Card>` (AGENTS rule 1). `Card` gained `asChild` (Radix `Slot`, same as `Button`) so the panel keeps its real `<section aria-labelledby>` landmark instead of being flattened to a div, and LOST shadcn's default `shadow-sm` — this app's card language is a flat hairline border (every hand-rolled surface, plus `ListingCard`, which lifts to `shadow-md` only on hover), which is part of why the primitive sat unused while surfaces re-declared the recipe around it. So the buyer's page is byte-identical after the migration: `--card-foreground` equals `--foreground` in both themes. Verified: 53 specs in `listing-detail.spec.ts` green (including the 3 new clipping matrices, the 2 renew specs and the contrast spec), plus `my-listings` · `manage-listing` · `conversations` · `i18n-digits` · `listing-action-bar` · `conversation-thread` · `reviews` · `navigation`; `tsc --noEmit`, eslint and `next build` clean.<br>**Review (cycle 17) — 7 findings closed.** The three copy/docs findings were already on disk from the aborted cycle 16 and were **verified, not rebuilt**: (a) the RTL reserve/sold wording — fa `listing.markReserved` was byte-identical to `listing.status.reserved` ("رزرو شده") and `markSold` to `status.sold`, so the panel's loudest control read as a STATE beside an "Active" pill, and ps borrowed the save/favourite root ("خوندي ښودل" ≈ "mark as saved") — now `ثبت رزرو`/`ثبت فروش` and `ریزرو کول`/`خرڅ ښودل`, mirrored in the mobile catalogs (`src/i18n/locales/{ps,fa}/listing.json`) per parity rule 2; (b) `layout/auth-nav.tsx` composes the unread count INTO its `aria-label` (`chat.unreadCount`), because an aria-label always beats an sr-only sibling; (c) the `ui/card.tsx` and `account/listing-actions.tsx` docstrings no longer assert invariants the tree does not honour — the card recipe names the ~20 surfaces still hand-rolling it and points at **R19** in REFACTOR_DUPLICATION.md, and the lifecycle table names the Duplicate/relist gap versus mobile instead of claiming a one-for-one mirror. Newly fixed: **(1) the count pill's salience.** Cycle 14's darkened dark `--primary-strong` (`hsl(217 91% 42%)`) won the digits ~6.6:1 but left the FILL at 3.0:1 against `--background` / 2.6:1 against `--card` — under the 3:1 non-text floor, and darker than the dark theme's own `--primary`, so "2 buyers are waiting on you" receded into the page at night while an AA-only spec stayed green. Dark now **inverts instead of darkening** (`hsl(217 91% 60%)` + a new `--primary-strong-foreground` = `var(--background)`): 5.5:1 for the digits and 5.5:1 against the page (4.1:1 against the `bg-secondary` button the panel's pill actually sits on). The contrast spec gained the second job — it walks up to the nearest OPAQUE ancestor and asserts fill-vs-surface >= 3:1 — mutation-verified: restoring the darkened fill with white ink passes the old 4.5:1 digits assertion and fails the new one at 2.23:1. **(2) ~40px of dead space for the owner.** Every control in `#listing-actions` self-hides for the seller, but the block still rendered its wrapper AND its inner `space-y-2` div: two zero-height boxes, so the column's `space-y-5` paid 20px on each side of nothing between the seller card and the description. The block is buyer-only, so it is now gated as a whole by `<HideForOwner>` (byte-identical for buyers/guests, and `<UnavailableActions>` stays a Server Component inside it); dropping the sentinel with it is safe because `<ListingActionBar>` self-suppresses for the owner anyway. Specced twice: `#listing-actions` is absent for the owner, and the general rule — no child of the detail column may have zero height without being `display:none`, the only state that forfeits the sibling margin. **(3) Edit demoted out of the action stack.** Two of the stack's four-to-five slots led to the same place (Manage → `/my-listings/[id]`, which carries its own Edit), so an expiring active listing stacked FIVE full-width bands (~240px of chrome) between the price block and the location card on a 375px phone. Edit moves to the heading row (`ghost`, `ms-auto`, still 40px — `link`'s `text-primary` measures 4.3:1 on this panel's own `bg-primary/10`, i.e. under AA for 14px), leaving the bands to what earns one: next transition (+ Renew when amber) + one way out + Chats-with-its-count. Chats keeps its band on purpose — it carries the number that pulls a seller back in. The two rows became one wrapping row, so the shape is 4 bands at <=375px and 2x2 from ~392px. New/updated specs in `e2e/listing-detail.spec.ts`: a cross-locale catalog invariant (per locale, the reserve/sold ACTION is never its own STATUS, and ps keeps clear of the save root — anchored so rewording `common.save` can't no-op it), an fa render of the panel read from the catalog, the fill-vs-surface floor, the dead-space pair and the Edit-demotion measurement (all mutation-verified). 55 specs in `listing-detail.spec.ts` green plus `listing-action-bar`; `tsc --noEmit`, eslint and `next build` clean. |
| W628 | Seller "away" banner | ✅ | Shared `away-banner.tsx` ("Seller is away until [date]", `PlaneTakeoff`, primary/info tone). Shown on `/listings/[id]` (buyer, `seller.sellerAwayUntil`), `/sellers/[id]` (public profile, enriched from listing detail), and the seller's own `/profile` (`profile.away.youAreAway`). Guards past dates client-side; reuses `is_away`/`away_until` from the serializer — no contract change. Owner-gated (`HideForOwner`) on **both** public surfaces — the listing page and `/sellers/[id]` — because "this seller is away" is a buyer's reply-time expectation; told to the seller it is the site describing them in the third person (they see `profile.away.youAreAway` instead). 3 locales; RTL + dark |
| L824 | Shareable listing link | ✅ | Shared `share-button.tsx` (Share2 + `common.share`) in the `/listings/[id]` badge row, all statuses. Web Share API when available (localized `listing.share.body` "{title} — {price}" + page URL); otherwise copies `window.location.href` + `common.linkCopied` sonner toast (label flips to `common.copyLink` on desktop). No contract change; 3 locales; RTL + dark |
| B2-STATE | Detail states (skeleton/not-found/sold) | ✅ | |
| SOLDNEXT | **Recovery CTAs on a sold/reserved listing** | ✅ | `unavailable-actions.tsx` replaces the flat grey notice on `/listings/[id]`. Keeps the status sentence (`soldNotice`/`reservedNotice`, falling back to `unavailableNotice` for a draft) and adds the two next steps a guest arriving from Google actually needs: PRIMARY "See similar in {category}" → `/bazaar?category=<slug>&min=<0.7×price>&max=<1.3×price>` and SECONDARY "More from {name}" → `/sellers/[id]`. The band is serialized with the shared `filtersToSearchString`, so the Bazaar sidebar renders category+min+max as active filters and the pill counts 3 — no new param vocabulary; Bazaar only queries `status: "active"`, so the dead listing can't reappear. Exported `priceBand()` drops `min`/`max` for a 0/null/negative price and never emits an inverted range. Each CTA self-omits when its data is missing (no link to nowhere). Reserved adds one muted "a reservation can fall through" line so the unchanged `SaveButton` below still reads as worthwhile. Props-only → **Server Component, zero client JS** on an indexed page. New keys `listing.detail.seeSimilarIn` / `moreFromSellerNamed` / `reservedMayFreeUp` in en/ps/fa (existing notice keys reused verbatim; `moreFromSeller` stays the rail heading). No contract change; RTL (`rtl:-scale-x-100` chevron, logical spacing) + dark; 6 Playwright specs<br>**Review fix-ups — 5 findings closed.** **(1) MUST: the band was currency-blind.** Rails' `price_at_least`/`price_at_most` filter the RAW `price` column with no currency scoping, and `Listing::CURRENCIES` is `AFN USD EUR` — so "see similar" on a $900 laptop asked the (overwhelmingly AFN) feed for 630–1,170 and landed on junk. `priceBand()` now takes the currency and emits a band **only for AFN**; every other currency gets a truthful category-only CTA. Adding a `currency` browse param was rejected on purpose: that is a new shared-contract vocabulary for one edge case, and the card fixes the param set. **(2) MUST: the primary CTA could itself land on an empty Bazaar** — a second, emptier dead end than the one this card removes, and reachable straight from the fixtures (listing 7, sold at AFN 8,000 in Clothes, banded 5,600–10,400 against a category whose only live item is a 1,200 jacket). The page now hands the card the live stock its CTA would show — the same `GET /listings/:id/similar` set the rails below render, so Next's per-render fetch memoization makes it ONE request, awaited only for a non-active listing — and the CTA degrades in three steps: band when the band provably holds stock → category alone when the category does → **no category CTA at all** when it doesn't (the seller CTA then takes the primary weight, so the sole action never reads as an afterthought). Rails' `by_category` is `self_and_children` for BOTH `similar_to` and the browse index, so non-empty similar ⇒ non-empty category feed. **(3) the owner gate** was already closed by TASK-WEB-OWN947's `HideForOwner` wrapper around `#listing-actions` — verified, not rebuilt (spec: "the sold/reserved recovery card is not shown to the owner"). **(4) copy + weight contrast:** `reservedMayFreeUp` no longer commands "save it" — it pointed at a `SaveButton` that lives outside the card — and states the fact instead ("A reservation can fall through, so this item may become available again."), all 3 locales; the second CTA drops from `outline`/40px to `ghost`/36px + muted label so the stack has a real primary. **(5) sold listings now read as archived** (DESIGN_SYSTEM §2 "grey, dimmed"): `ListingGallery` gained `dimmed` (opacity only — no filter, true in both themes) and `PriceTag` gained mobile's `tone` prop (`default` \| `muted`), both set for `sold` ONLY, so a reserved item keeps its full-strength photo and price. The band maths moved to a **pure** `components/listing/recovery-band.ts` (`priceBand` + `recoveryBand`) so its edge cases are pinned by 7 unit specs in `e2e/recovery-band.spec.ts` — rounding, 0/null/negative/NaN/Infinity, inversion, per-currency, band-hits-stock — with no browser in the loop. `e2e/listing-detail.spec.ts`'s recovery block grew to 12 browser specs (both fixture bands, the click-through proving real results and no "No listings found", the USD case, the empty-category case, the sold dim, en/ps/fa copy read from the catalogs, no horizontal scroll at 375px in en + ps) on two new mock fixtures (12: sold in USD; 13: sold in a category with zero live stock) kept out of the public feed like `EXPIRED_MINE`. 99 specs across `listing-detail` · `listing-action-bar` · `bazaar` · `bazaar-filters` green; `tsc --noEmit` and eslint clean. **`npm run build` fails on `/404` — pre-existing and unrelated** (`<Html> should not be imported outside of pages/_document` while prerendering `/_error`; reproduced on a clean stashed tree, no app code of this card involved). MOBILE MIRROR (`ListingDetail.tsx`) is still open. |

## C — Seller: Listings & Lifecycle

| ID | Feature | Web | Migration note |
|---|---|---|---|
| C1 | Create / edit listing (sectioned form) | ✅ | `/listings/new`, `/listings/[id]/edit` |
| C1-PHOTO | Photo upload + reorder | ✅ | ⚠️ web = `<input type=file>`/drag-drop, not native camera |
| C1-CAT | Category picker | ✅ | |
| C1-PROV | Province picker | ✅ | |
| C1-DRAFT | New-listing draft autosave + restore | ⬜ | Persist unsent form to localStorage; restore/discard banner on reopen |
| C2 | My Listings (My Shop) + status tabs | ✅ | `/my-listings` |
| M826 | Per-status tab counts | ✅ | All six tabs carry their count (hidden at zero) in the `/my-listings` SegmentedControl, localized digits, recomputed off the same `['my-listings']` query — verified by spec |
| C2-* | Lifecycle: publish/reserve/sold/unpublish/delete | ✅ | full lifecycle on `/my-listings/[id]` **and inline on every `/my-listings` card** (TASK-WEB-C2-ACTIONS, mobile parity with `SellerListingCard`). The shared brain lives in `account/listing-actions.tsx` — `LIFECYCLE` copy map + `actionsFor()` + `useListingLifecycle()` (which owns the pending-action state machine, the mutation, toasts, invalidation and the `busy` flag) + `<LifecycleDialogs>` (the confirm-vs-buyer-picker prompt pair) — imported by BOTH the detail view and the card footer, so a surface only supplies its own buttons: no handler, dialog or copy is written twice. The card footer (`account/seller-listing-actions.tsx`) renders one primary Button + a kebab (`ui/dropdown-menu`) with the remaining transitions, Edit and a destructive Delete. The post-sale `ReviewPromptDialog` is owned by the LIST (`seller-listings-view.tsx`), not the card: the `['my-listings']` refetch that follows a sale drops the now-sold card out of a filtered tab, which would unmount a prompt living in the card before the seller could rate anyone. `ListingCard` gained an optional `footer` prop (rendered outside the anchor) and `ListingGrid` a `footerFor` — extended, not forked. No new endpoints, no new i18n keys; RTL (`dir` on the menu, logical spacing) + dark; both controls hold the house **40px tap target** (`min-h-10` on the primary, which stays `h-auto` so a long label wraps instead of clipping; `size-10` on the kebab) — the same floor the Report/Share pair was raised to. 29 test blocks / 34 runtime Playwright specs in `e2e/my-listings.spec.ts` (per-status primary, kebab contents, each confirm path, buyer picker, buyer-recorded sale → review prompt, in-place refetch with no skeleton flash or reload, failed action → error toast + unchanged card, action row disabled while in flight, the `ps` RTL row + mirrored menu, the status filter's tablist roles + 40px options, a stale-`expired`-flag listing bucketed under Expired, a failed load printing no "0 listings" and no filter row, a photoless card captioned in the seller's shop but NOT on either buyer-facing surface — the feed and the public seller profile's Sold grid, which also sets `showStatus` — and the 40px targets fitting a 2-column card at **375px and 360px in all three locales** — en is the binding width there, ps/fa are the roomy RTL cases; mutation-verified). Every seller surface now imports this one brain — `account/manage-listing-view.tsx`, `account/seller-listing-actions.tsx`, `listing/owner-listing-bar.tsx` and `chat/conversation-thread.tsx` — so `LIFECYCLE`/`actionsFor`/the mutation exist in exactly one file (verified by grep). Design-review follow-ups: the primary is `font-semibold sm:text-sm` (it must not be quieter than the card's meta line — mobile's is 13px/700); a terminal `sold` card renders the trigger full-width WITH the "More options" label instead of a lone unlabeled glyph (mobile parity — `SellerListingCard` does the same, and Edit/Delete live behind it); beside a primary, though, the trigger is a bare icon kebab — a **deliberate deviation** from mobile, which keeps the word in BOTH shapes (its compact More is a 92pt labelled button): a labelled More cannot sit next to "Mark as Sold" in a 2-column card at 375px, and the kebab is the native web idiom (the title-bearing `aria-label` keeps the six distinguishable), so do not "restore parity" here; the primary also carries `listing.detail.actionFor` as its accessible name so seven "Mark as Sold" buttons are told apart by a screen reader; `ListingCardSkeleton`/`ListingGridSkeleton` gained `withFooter` so the pending placeholder reserves the action row's height (no card-height jump + grid reflow when `['my-listings']` lands); the failed-load state is the house `errorTitle`/`errorDescription`/`retry` EmptyState wired to `refetch()`, not a bare "Error"; and `ui/dropdown-menu`'s items are `min-h-10 py-2` app-wide, since on a phone this kebab is the only path to Unpublish/Renew/Edit/Delete. Second design-review pass (the screen AROUND the cards, which the action row exposed): the six status filters are the shared `shared/segmented-control.tsx` — extended with a `wrap` variant, because six options with counts cannot fit one `inline-flex` row on a phone — so they carry `tablist`/`tab`/`aria-selected`, the focus ring and the same 40px floor instead of the 32px hand-rolled pills they were; `matchesTab()` buckets Active vs Expired with the SHARED `listingExpiryState()` rule (not the raw `expired` flag, which Rails leaves stale until something touches the record — a lapsed listing used to sit under Active while its own card showed a red "Expired" pill and offered Renew, with the Expired tab reading (0)); tab counts are localized through `format.number` so they match the "{count} listings" line above them; and that line is gated on real DATA, not `!isPending` — in TanStack v5 `isPending` is false in the error state too, so a failed load printed "**0 listings**" above "Something went wrong" on the seller's inventory of record (the filter row is hidden on error as well, matching Hidden/Saved). And `shared/remote-image.tsx` gained an optional `label` (`text-xs`, the bottom of the house scale — mobile's two equivalents are 11px) for its no-photo tile, which `ListingCard` passes as the existing `listing.noPhoto` key under its own **`nameMissingPhoto`** flag — set ONLY by `account/seller-listings-view.tsx`. In the seller's own shop a photoless listing is an action prompt (no photo is why nobody is messaging them, which mobile's `SellerListingCard` states in words); every buyer-facing grid keeps the quiet unlabelled glyph. The flag is deliberately NOT `showStatus`: that prop means "show the lifecycle badges" and is also on for the public seller profile's Sold grid (`seller/seller-listings-tabs.tsx`), where riding it stamped "No photo" on every photoless sold card a buyer scrolled past. Suppressing the caption for buyers is itself a **deliberate divergence** from mobile — mobile's shared `ListingCard` captions a photoless listing unconditionally, so its buyer feed is captioned too — so do not "restore parity" by dropping the gate (noted in the component as well). Both public surfaces are asserted absent by spec (the mock API gained a `GET /users/:id/sold_listings` route so the Sold grid is reachable in E2E at all). One new i18n key in all 3 locales: `listing.filter.label` (the tablist's accessible name) |
| C3 | Expiry + **Renew** + Expired tab | ❓ | Renew action is ✅ (parity); verify the **Expired tab** + expiry badge exist, add if missing |
| C-DETAIL | Owner listing detail + manage | ✅ | |
| N802 | 7-day views sparkline analytics | ✅ | views chart on `/my-listings/[id]` |
| C-LCONV | Listing conversations (seller) | ✅ | `/conversations?listing=<id>` |
| W713 | Seller "away mode" (set away-until) | ✅ | Away-mode section in the `/profile/edit` shadcn form: checkbox toggle + native date picker (min=today). On save, away-on + date → `awayUntil = <date>T23:59:59Z`; away-off → `awayUntil: null`; away-on w/ no date → omit (mirrors mobile). Reuses `PUT /users/me` (`awayUntil`) via the existing `/api/me` allow-list — no contract change. Drives the W628 banner. 3 locales; RTL + dark |

## D — Chat

| ID | Feature | Web | Migration note |
|---|---|---|---|
| D1 | Conversations list (thumb/preview/unread) | ✅ | `/conversations`. **Paginated (TASK-WEB-INBOX20):** `getConversations()` used to return `d.conversations` and discard `meta.pagination`, so the list was silently capped at Rails' 20-per-page — a user with 25 threads could not reach 5 of them on any tab. It now returns `{ items, pagination }` (the shape `getListings` already returns) and the view is a `useInfiniteQuery` + the same Load-more `Button` as `seller/reviews-section.tsx`; the archive and mark-read optimistic mutations patch the `{pages:[…]}` cache instead of a flat array, so they still flip + roll back + toast for a row that only page 2 has. The buyer picker (`sell-buyer-dialog`) and the duplicate-conversation recovery (`start-conversation-button`) deliberately read page 1 only. |
| Z684/J471 | **Inbox list search (name / listing / preview)** | ✅ | The web inbox had no way to FIND a thread even though the thread already shipped the highlighter. Now a shared `SearchField` above the list filters the LOADED rows through `src/lib/filter-conversations.ts` (port of mobile's `filterConversations`): counterpart name (`otherParticipant ?? buyer ?? seller`), `listing.title`, and the rendered preview — via the new `src/lib/conversation-preview.ts` (mobile's `conversationPreviewText`), which the row renders too, so search can only ever match text on screen. Both sides are digit-normalized (`src/lib/normalize-digits.ts`), because the offer preview now formats its amount through `formatPrice` — so `75000` matches a `؋ ۷۵٬۰۰۰` row on /ps and `۷۵۰۰۰` matches `AFN 75,000` on /en (`chat.preview.offer`/`counterOffer` take ONE `{price}`, mobile's wording, instead of a raw `{amount} {currency}`). Matches are highlighted with the thread's own `splitHighlight`, now behind the shared `components/shared/highlighted-text.tsx` that `message-bubble` also uses. No-match → `EmptyState` + `SearchX` + clear action, with the description admitting unloaded pages while `hasNextPage`. Render-only: never refetches, never touches an unread badge. Server-side search and role scoping (TASK-R517) are out of scope |
| D1-BADGE | Aggregate unread badge | ✅ | |
| K741 | Mark read/unread from the list | ✅ | Per-row kebab item on `/conversations`: "Mark as read" when unread, "Mark as unread" otherwise. Optimistic flip of the row's `unreadCount` (0/1) + rollback on error; invalidates the conversations list and refreshes the user so the header aggregate unread badge stays in sync. Reuses `PUT /conversations/:id/mark_read`·`/mark_unread` (added to the `/api/me` allow-list). Keys mirror mobile `chat.actions.markRead/markUnread/markReadError`; RTL + dark |
| A618 | Archive / unarchive conversation | ✅ | Inbox/Archived segmented toggle on `/conversations` + per-row kebab (Archive in inbox, Unarchive in archived). Optimistic row-removal + rollback on error; invalidates both partitions so the moved row lands on the other tab. Reuses `PUT /conversations/:id/archive`·`/unarchive` (added to `/api/me` allow-list) and `GET /conversations?archived=true`. 3 locales (keys mirror mobile `chat.tabs`/`chat.archive`); RTL + dark |
| D2 | Conversation thread (bubbles, RTL) | ✅ | `/conversations/[id]` |
| D2-READ | Read receipts (double-tick) | ✅ | Gap was real: bubbles rendered no `createdAt`/`readAt`. Every text / attachment / offer / counter / meetup bubble now ends in a meta row — `formatTime(createdAt)` + (on my own messages) a `Check` (sent) / `CheckCheck` (seen) tick titled from `chat.message.sent`·`seen`. Centred system + accepted/declined pills stay bare, mirroring mobile; the deleted tombstone stays bare too (mobile prints a time there — web keeps a retracted message to one quiet line). Thread inserts a day separator between calendar days (`lib/message-days.ts` `dayKey`, local not UTC), labelled `chat.day.today`/`yesterday` else `formatDate`; suppressed while in-thread search is active. Separators mark a day *boundary* only, so a single-day thread shows none — mobile's `buildThreadRows` additionally emits one above the first message, a deliberate divergence (the web thread already has the pinned listing header there). No API change (`read_at`/`created_at` already serialized; `mark_read` already wired). The tick flips on the sender's next fetch — Rails broadcasts new messages only. 3 locales · RTL · light/dark · `e2e/chat-receipts.spec.ts` |
| D2-CABLE | Real-time updates (ActionCable) | ✅ | ports directly — web already uses `@rails/actioncable` |
| D3-MEET | Meetup propose + accept/decline | ✅ | |
| D3-OFFER | Offer accept/decline | ✅ | |
| O829 | Counter-offer | ✅ | Seller counters a buyer's offer with a new price from the thread. A "Counter" outline button shows on the buyer's original `offer` bubble only to the seller when it's unanswered; opens a shadcn dialog seeded with the buyer's amount. Sends an `offer_counter` message (same kind mobile uses) reusing the pipe-encoded `amount\|currency\|listedPrice` body over the existing `POST /conversations/:id/messages` — no contract change. Counter bubbles render with the ArrowLeftRight icon + `counterLabel`/`counteredAt` on both sides, and support accept/decline like a normal offer. 3 locales · RTL · light/dark |
| G083 | Offer quick-amount chips | ✅ | Make-offer dialog shows 95/90/85% chips (`shared/offer-quick-chips.tsx`, same `computeChipAmount` as mobile); tap fills the amount without sending, matching chip highlighted, hidden when price is 0/null. Reusable for the in-thread counter composer |
| M482 | In-chat photo/file messages | ✅ | web file upload (photo + document) |
| M913 | **Delete / retract message** (tombstone) | ✅ | Own-message delete behind confirm-dialog; optimistic tombstone + rollback; live flip over ActionCable. Reuses `DELETE /conversations/:id/messages/:id` |
| Q374 | Quick-reply presets | ✅ | Localized canned-phrase chips above the composer on `/conversations/[id]` (shared `quick-replies.tsx`). Buyer/seller phrase set chosen by whether the current user is the listing seller; tapping a chip appends the phrase to the draft (space-separated) and focuses the input — no auto-send. Reuses the mobile `chat.quickReplies.*` keys verbatim in en/ps/fa; hidden on a closed conversation; RTL + dark. No API/contract change |
| C739 | Composer draft persistence (per-convo) | ✅ | Unsent composer text on `/conversations/[id]` persists to a per-conversation localStorage key (`hatiwal.chat.draft:<id>`) via `use-composer-draft.ts` (web port of mobile's `useComposerDraft`): hydrates on open/switch, debounced ~400ms write (flushed on navigate-away), key removed on clear/successful send. Disabled for closed conversations; localStorage failures (private mode) degrade to in-memory. No API/i18n change |
| N803 | **Message search within thread** | ✅ | Header search toggle expands an input; filters loaded messages (text, non-deleted, case-insensitive), highlights matches, shows "X of Y" + loaded-only note. Client-side; RTL + dark |
| F084 | Lifecycle actions from chat header | ❓ | Verify seller can Reserve/Mark-sold from the pinned listing header |
| D2-BLOCK | Block / unblock in thread | ✅ | |
| R483 | Report participant from thread | ✅ | Already shipped: the shared `ReportButton` (icon-only variant) sits in the `/conversations/[id]` header beside the block shield, reporting the other participant as `User`. Verified by `e2e/report.spec.ts` ("Report → block from the conversation thread"); row was stale |
| D2-CLOSED | Closed-conversation state | ❓ | Verify input is disabled with a notice when a conversation is closed |

## E — Saved / Favorites

| ID | Feature | Web | Migration note |
|---|---|---|---|
| E1 | Saved list (grid + infinite scroll) | ✅ | `/saved` |
| E1-TOGGLE | Save/unsave consistency everywhere | ✅ | |
| V836 | Recently viewed (`my/viewed_listings`) | ⬜ | Re-engagement page of listings the buyer viewed |

## F — Profile & Account

| ID | Feature | Web | Migration note |
|---|---|---|---|
| F1 | My profile (stats, entry points) | ✅ | `/profile` |
| F1-MODE | Buyer / seller mode toggle | ✅ | |
| F1-THEME | Theme toggle (light/dark) | ✅ | |
| REP815 | **Own rating + "My reviews" on `/profile`** | ✅ | Your reputation on your own profile (mobile `Profile.tsx` header rating + Reviews quick action → `AllReviews`). `RatingDisplay` renders in the new `UserIdentity` `meta` slot — inside the text column, under your name, at default `sm` so the name stays loudest — wrapped in a thumb-sized (`min-h-10`) link to `#my-reviews` whose accessible name is score + count + destination (no `aria-label`, which would have swallowed the first two). That anchor is the shared `reviews-section.tsx` reused verbatim with `title={t('reviews.myReviewsTitle')}` + `headingSize="sm"` (matching the page's other `h2`s): As-seller / As-buyer toggle, load-more, skeleton, role-specific empty state. **One score per page, everywhere:** `ReviewsSection` never renders a summary — the score always lives in the page header next to the person (`sm` under your name on `/profile`, `lg` hero on `/sellers/[id]`), so `showSummary` is gone rather than passed `false`. `UserIdentity` also gained `nameAs`, making the person's name the `h1` on both profile pages (they had none), and it now drops `meta` when `href` is set — the docblock always promised that, but the code nested an `<a>` in an `<a>`. Data is already in the payload — `UserSerializer` `:me` serves `avgRating`/`reviewCount` — and the list reuses the public `GET /users/:id/reviews` (plain `User.find`, no `publicly_active` gate, so your own id works). No API/contract change. "View my public profile" → `/sellers/[id]`, hidden while `deletionScheduledAt` is set (Rails scopes that page to `User.publicly_active` → would 404). 3 locales (`reviews.emptyAsSeller`/`emptyAsBuyer` added) · RTL (logical props, no left/right) · light+dark · e2e in `e2e/profile.spec.ts`. **One digit system per locale**, closed in review: putting three numbers on one page exposed that the score printed `avgRating.toFixed(1)` (locale-blind — the only `toFixed` in `src/`), the stat tiles printed `{value}` raw, and `ReviewCard` dated through `useFormatter()` with the raw `ps` tag V8 has no data for. So `/ps/profile` showed a Latin `4.7` inside the same link as an Arabic-Indic `۳ نظرونه`, three Latin tiles, and English review dates. All three now go through `src/lib/format.ts` (`formatNumber`, which gained an `options` arg for the one-decimal rating; `formatDate`) — mobile already formats the same score through `useLocalization().formatNumber`. Fenced by a `/ps/profile` case in `e2e/i18n-digits.spec.ts` |
| F2 | Edit profile (+ avatar upload) | ✅ | `/profile/edit` |
| F2-LOC | Profile map location | ✅ | ⚠️ browser Geolocation for "use my location" |
| F3 | Public seller profile | ✅ | `/sellers/[id]` |
| F742 | Seller **sold-items showcase** tab | ✅ | Active/Sold segmented control on `/sellers/[id]` (`seller-listings-tabs.tsx`). Active grid is server-rendered; Sold grid lazy-loads via TanStack Query on first Sold-tab open, reusing the guest-accessible `GET /users/:id/sold_listings` (same endpoint mobile uses) — no contract change. Sold cards show a dimmed status badge; empty state when none; 3 locales; RTL + dark |
| V613 | **Verified seller badge** | ✅ | Shared `verified-badge.tsx` (BadgeCheck, `common.verified` title, `text-primary` token) reused via `UserIdentity` at all three sites — listing detail seller card (`/listings/[id]`), public seller profile (`/sellers/[id]`), and conversation thread header. Driven by the existing `verified` boolean on the User payload; no contract change. 3 locales · RTL · dark |
| A356 | "Active recently" label | ⬜ | Privacy-safe last-active label on public profile |
| S392 | Shareable seller-profile link | ✅ | Same shared `share-button.tsx` in the `/sellers/[id]` profile header row (beside Report), body `seller.share.body` ("Check out {name} on Hatiwal", mirrors mobile `profile.share.body`). Web Share API → native sheet; fallback copy-link + toast. 3 locales; RTL + dark |
| B047 | Blocked-users management | ✅ | `/settings/blocked-users` |

## G — Safety, Reporting & Moderation

| ID | Feature | Web | Migration note |
|---|---|---|---|
| G1 | Report listing / user (6 reasons) | ✅ | from detail + seller profile |
| R612 | Report → block follow-up | ✅ | Closed inside the one shared `components/shared/report-button.tsx`, so all three surfaces get it at once (listing detail, `/sellers/[id]`, conversation-thread header). A successful **User** report keeps its success toast, closes the report dialog, then opens the house `ConfirmDialog` (`report.block.*`, destructive) → confirm fires the existing `blockUser()` (`POST /users/:id/block`, already allow-listed) and toasts; a failed block toasts the error and the report still stands (never rolled back). A **Listing** report never prompts. Both writes are React Query mutations; the block invalidates `["blocked-users"]`, `["conversations"]`, `["conversation"]`. "Already blocked?" is answered by the shared `["blocked-users"]` query (`GET /blocks` = the people *I* blocked, fetched only while the dialog is open), so the three surfaces behave identically — never by the conversation's `blockedWithParticipant`, which is true in EITHER direction and so would have denied the block to whoever was being harassed. `onBlocked` still flips the thread header's shield to Unblock without waiting for the refetch. Client-only — no API, no contract change. 3 locales · RTL · dark. E2E: `e2e/report.spec.ts` (11 tests) |
| R739 | "My Reports" status screen | ⬜ | See your submitted reports + their outcome |
| G-WARN | Moderation warnings banner | ✅ | profile warnings banner |
| G-BLOCK | Block / unblock user | ✅ | |

## Cross-cutting / Polish

| ID | Feature | Web | Migration note |
|---|---|---|---|
| P1 | Animation system | ❓ | Web transitions differ (CSS/Framer). Match spirit, not RN Reanimated 1:1 |
| D094 | Contextual empty-state illustrations | ⬜ | Port the per-section empty illustrations (Browse/Saved/Chat/My-Shop) |
| T702 | Persistent data cache | ❓ | Web uses TanStack Query; add a persister only if data-loss on reload is a real issue |
| AUTHCACHE | Per-viewer cache hygiene on auth change | ✅ | Mobile resets its stores + AsyncStorage on logout; web had no equivalent, so one tab could serve the previous user's `['saved-listings']` / `['my-listings']` / `['conversations']`. Now `queryClient.clear()` on every login/register/logout transition (`auth-provider.tsx`). Fixed in `8b534cd`, regression-covered by `e2e/auth.spec.ts` |
| S0 | Shared component library | ✅ | Web has its own shadcn/ui equivalents |

---

## 📱 Mobile-only — do NOT port 1:1

| ID | Feature | Why / web equivalent |
|---|---|---|
| N801 | Push-notification token registration | `expo-notifications` device tokens. Web = Web Push / service workers (separate rebuild, not a port) |
| C1-PHOTO / M482 | Native camera capture | Web uses `<input type=file>` / drag-drop (the *upload* itself is ✅ on web) |
| B5 / F2-LOC | Device GPS permission | Web uses the browser Geolocation API (different permission model) |
| B2-PHONE | Tap-to-call dialing | Web = `tel:` link / copy number |
| P1-haptics | Haptic feedback | No web equivalent (no-op) |
| L824 / S392 | Native share sheet | Web = Web Share API / copy-link |
| A-store | Secure token storage (Keychain) | Web = httpOnly cookies (already implemented) |
| Q3 / Q5 | iOS/Android platform guards + device test matrix | Native-only concern |
| T701 | Maestro Android E2E CI | Web = Playwright/Cypress instead |

---

## Migration work queue (the ⬜ gaps → `web` board tasks)

Prioritized. Each becomes a `type: web` card on `hatiwal-mobile/docs/SPRINT_BOARD.md` for the software
house. **First batch seeded** = the recent post-audit features with the clearest user value:

**P1 — recent features users already have on mobile**
- ~~`WEB-M913` Delete/retract chat message → tombstone~~ ✅ shipped
- ~~`WEB-D583` Nearest-first sort on Bazaar~~ ✅ shipped
- ~~`WEB-V259` Saved-by-N social proof on listing detail~~ ✅ shipped
- ~~`WEB-N803` In-thread message search~~ ✅ shipped
- ~~`WEB-N804` Price-drop badge~~ ✅ shipped
- `WEB-N805` Seller response-rate badge

**P2 — trust & seller tooling**
- `WEB-C2-THICK` Thicker seller cards on `/my-listings` — views + conversations counts **and** the compact
  "Reserved for {name}" / "Sold to {name}" line: the new inline quick-actions let a seller reserve/sell from
  the card, but the card then says only "Reserved", never for WHOM. Needs a `sale` field on the listing
  payload + `listing.sale.*` in en/ps/fa. Full scope: the "Thicker seller list cards" item in
  `../../docs/MOBILE_WEB_PARITY.md`
- `WEB-C2-SEARCH` Search within My Shop — mobile has it, `seller-listings-view.tsx` has status tabs only
- ~~`WEB-V613` Verified seller badge~~ ✅ shipped · ~~`WEB-F742` Sold-items showcase~~ ✅ shipped · `WEB-A356` Active-recently label
- ~~`WEB-W713`/`WEB-W628` Seller away mode + banner~~ ✅ shipped · `WEB-N071` Firm/negotiable badge · `WEB-C3b` Expiry badge

**P2 — chat depth**
- ~~`WEB-O829` Counter-offer~~ ✅ shipped · ~~`WEB-K741` Mark read/unread from list~~ ✅ shipped · ~~`WEB-A618` Archive conversation~~ ✅ shipped
- ~~`WEB-Q374` Quick-reply presets~~ ✅ shipped · ~~`WEB-C739` Composer draft~~ ✅ shipped · ~~`WEB-R483` Report from thread~~ ✅ shipped

**P3 — discovery & re-engagement**
- `WEB-B931` Most-viewed sort · `WEB-B617` Active-sellers filter · `WEB-B6` Seen indicator
- `WEB-B-VIEW` Grid/list toggle · ~~`WEB-S417` Subcategory drill-down~~ ✅ shipped · `WEB-N612` Saved-search new-match badge
- `WEB-V836` Recently viewed · `WEB-C1-DRAFT` New-listing draft autosave · `WEB-D094` Empty illustrations
- ~~`WEB-R612` Report→block follow-up~~ ✅ shipped · `WEB-R739` My Reports screen · `WEB-W924` First-visit welcome

**Verify-first (❓ — confirm the gap before building):** `C481`, `M826`, `C3` Expired tab,
~~`D2-READ`~~ ✅ shipped (the gap was real), `G083`, `F084`, `D2-CLOSED`.

> Housekeeping: as each `web` task ships, flip its row above from ⬜ to ✅ and update
> `../../docs/MOBILE_WEB_PARITY.md` so the two docs never drift.

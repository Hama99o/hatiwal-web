# Secrets for Kamal deployment — loaded from .env.production.
# DO NOT enter raw credentials here. Pull from ENV or files. Safe to commit.
#
# Only the registry password is a true secret for the web app; everything else
# (public URLs, host) is plain config passed via .env.production / build args.

KAMAL_REGISTRY_PASSWORD=$(grep KAMAL_REGISTRY_PASSWORD .env.production | cut -d= -f2-)
